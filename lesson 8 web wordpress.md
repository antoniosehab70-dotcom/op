---

# WordPress وWPScan — الشرح الكامل

---

## أولاً: الفكرة العامة

الشرح ده بيغطي إيه هو الـ WordPress، ليه مهم في الـ Pentesting، وإزاي بنستخدم أداة الـ WPScan عشان نكتشف ثغراته — مع شرح الخطوات اللي بعد الـ Scan.

---

## ثانياً: الشرح الكامل بالعربي

---

### 📌 الجزء الأول: إيه هو WordPress؟

الـ **WordPress** (ووردبريس) هو **CMS — Content Management System** (كونتنت منجمنت سيستم) — يعني نظام إدارة محتوى. ببساطة هو برنامج جاهز بيخلي أي حد يعمل موقع ويب من غير ما يكتب كود من الصفر.

بدل ما المطور يكتب كل حاجة يدوياً — بيثبّت WordPress على السيرفر وبيبدأ يضيف محتوى من لوحة تحكم بسيطة.

**أرقام مهمة جداً:** أكتر من **43%** من كل مواقع الإنترنت شغّالة على WordPress. ده معناه إن لو اتعلمت تختبر WordPress — عندك هدف محتمل في كل موقعين من كل خمسة.

**WordPress مبني على:**

- **PHP** (بي إتش بي) — لغة البرمجة اللي بيشتغل بيها من جانب السيرفر
- **MySQL** (ماي إس كيو إل) — قاعدة البيانات اللي بتخزّن فيها كل حاجة
- **HTML / CSS / JavaScript** — للواجهة اللي المستخدم بيشوفها

---

### 📌 الجزء الثاني: هيكل WordPress — إيه اللي جوّاه؟

لما بتيجي تختبر WordPress لازم تعرف هيكله عشان تعرف فين الثغرات.

**WordPress عنده ثلاث عناصر رئيسية:**

**١. الـ Core (كور) — النواة** ده الكود الأساسي بتاع WordPress نفسه. بيتحدّث بانتظام. لو الموقع مش محدّث — الـ Core نفسه ممكن يكون فيه ثغرات.

**٢. الـ Plugins (بلجنز) — الإضافات** دي إضافات بيحطها صاحب الموقع عشان يضيف features. زي Plugin للـ Contact Form، أو Plugin لعمل Online Store، أو Plugin للـ SEO. وده **أخطر جزء** — لأن أي Plugin مش متحدّث أو مكتوب بشكل غلط ممكن يكون فيه ثغرة. الـ WPScan بيدور على الثغرات دي تحديداً.

**٣. الـ Themes (ثيمز) — القوالب** ده شكل الموقع — التصميم والألوان. زي الـ Plugins، ممكن يكون فيه ثغرات لو مش متحدّث.

---

### 📌 الجزء الثالث: ليه WordPress هدف مهم في الـ Pentesting؟

لأن معظم أصحاب المواقع مش بيحدّثوا الـ Plugins باستمرار. فبيفضل عندهم Plugins قديمة فيها ثغرات معروفة ومنشورة. المهاجم بس محتاج يعرف:

- إيه الـ Plugins الموجودة؟
- إيه إصداراتها (Versions)؟
- هل في ثغرات معروفة في الإصدارات دي؟

وده بالظبط اللي بتعمله **WPScan**.

---

### 📌 الجزء الرابع: إيه هو WPScan؟

الـ **WPScan** (ووردبريس سكان) هو **WordPress Security Scanner** (ووردبريس سيكيوريتي سكانر) — أداة مخصصة لاختبار مواقع WordPress. مثبّتة أصلاً على Kali Linux.

**بتعمل إيه WPScan؟**

- بتكتشف إصدار WordPress المستخدم
- بتجمع الـ Plugins وإصداراتها
- بتجمع الـ Themes وإصداراتها
- بتجمع أسماء المستخدمين (Usernames)
- لو عندك **API Token** — بتجيبلك الثغرات المعروفة في كل Plugin

---

### 📌 الجزء الخامس: الـ API Token — إيه ده وليه مهم؟

الـ **API Token** (أيه بي آي توكن) هو مفتاح سري بتاخده من موقع WPScan بعد ما تعمل حساب مجاني على **wpscan.com**.

**من غير API Token:** الأداة بتجمع المعلومات بس — بتقولك "في Plugin اسمه X إصدار 2.1" لكن مش بتقولك في ثغرات ولا لأ.

**مع API Token:** الأداة بتروح لقاعدة بيانات الثغرات الخاصة بيهم وبتقولك "Plugin X إصدار 2.1 فيه ثغرة CVE كذا نوعها XSS أو SQL Injection".

---

### 📌 الجزء السادس: شرح الأمر اللي في الصورة

```bash
wpscan --url https://5g4drdab7f8zo0fnqs20c2sr7.us-east-10.attackdefensecloudlabs.com/ --enumerate p --plugins-detection aggressive
```

**تقطيع الأمر:**

`wpscan` ← تشغيل الأداة

`--url https://...` ← الـ URL بتاع الموقع اللي هتسكنه

`--enumerate p` ←
اعمل **Enumerate** (إنيوميريت) — يعني جمع معلومات — وهنا `p` معناها **Plugins** بس

`--plugins-detection aggressive` ← 
استخدم الـ **Aggressive Mode** (أجريسيف مود) في اكتشاف الـ Plugins — يعني بيجرّب أكتر عدد ممكن من الطرق عشان يلاقي كل Plugin موجود حتى لو مش ظاهر بشكل واضح

**قيم الـ enumerate التانية اللي ممكن تستخدمها:**

`p` ← Plugins فقط

`t` ← Themes فقط

`u` ← Usernames فقط

`ap` ← كل الـ Plugins

`at` ← كل الـ Themes

`vp` ← الـ Plugins اللي فيها ثغرات بس (محتاج API Token)

---

### 📌 الجزء السابع: الخطوات الكاملة من الأول للآخر

---

**الخطوة ١ — جمع معلومات عامة عن الموقع**

```bash
wpscan --url https://target.com
```

بيجيبلك: إصدار WordPress، الـ Theme الأساسي، معلومات عامة.

---

**الخطوة ٢ — اكتشاف الـ Plugins**

```bash
wpscan --url https://target.com --enumerate p --plugins-detection aggressive
```

ده اللي عملته في الصورة — بيجيبلك كل الـ Plugins وإصداراتها.

---

**الخطوة ٣ — اكتشاف المستخدمين**

```bash
wpscan --url https://target.com --enumerate u
```

بيجيبلك أسماء المستخدمين المسجلين — مهم جداً لو هتحاول تعمل Brute Force على الـ Login.

---

**الخطوة ٤ — إضافة الـ API Token لاكتشاف الثغرات**

```bash
wpscan --url https://target.com --enumerate p --plugins-detection aggressive --api-token YOUR_TOKEN_HERE
```

دلوقتي بدل ما تقولك "Plugin X موجود" — بتقولك "Plugin X فيه ثغرة من نوع كذا CVE كذا — خطورتها High".

---

**الخطوة ٥ — بعد ما تعرف الثغرة**

لما WPScan تقولك في ثغرة في Plugin معين، بتاخد اسم الـ Plugin والإصدار والـ CVE وبتروح تدور على الـ Exploit بتاعه في:

- **searchsploit** — اللي اتعلمته قبل كده
- **exploit-db.com**
- أو بتستخدمه مباشرة لو Metasploit عنده Module ليه

---

## ثالثاً: نطق المصطلحات الصعبة — في جدول المصطلحات

---

## رابعاً: قاموس المصطلحات

|المصطلح|المعنى|النطق|
|---|---|---|
|WordPress|نظام إدارة محتوى مواقع — الأشهر في العالم|ووردبريس|
|CMS — Content Management System|نظام إدارة المحتوى|كونتنت منجمنت سيستم|
|PHP|لغة برمجة السيرفر اللي WordPress مبني عليها|بي إتش بي|
|MySQL|قاعدة البيانات بتاعة WordPress|ماي إس كيو إل|
|Core|النواة — الكود الأساسي لـ WordPress|كور|
|Plugins|إضافات بتضاف للموقع لتوسيع قدراته|بلجنز|
|Themes|قوالب التصميم البصري للموقع|ثيمز|
|WPScan|أداة اختبار أمان مواقع WordPress|ووردبريس سكان|
|WordPress Security Scanner|ماسح أمني متخصص في WordPress|ووردبريس سيكيوريتي سكانر|
|Enumerate|جمع وحصر المعلومات بشكل منظم|إنيوميريت|
|API Token|مفتاح سري للوصول لقاعدة بيانات الثغرات|أيه بي آي توكن|
|Aggressive Mode|وضع مكثف في الفحص — بيجرّب أكتر الطرق|أجريسيف مود|
|CVE|رقم معياري لتسجيل الثغرات المكتشفة|سي في إي|
|Version|إصدار البرنامج أو الـ Plugin|فيرجن|
|Username Enumeration|جمع أسماء المستخدمين من الموقع|يوزرنيم إنيوميريشن|
|Brute Force|تجربة كلمات مرور كتير تلقائياً|بروت فورس|
|Exploit|كود يستغل ثغرة معينة|إكسبلويت|

---

## خامساً: الملخص النهائي

**WordPress** 
نظام إدارة محتوى مبني على PHP وMySQL — بيشغّل 43% من الإنترنت — وبيتكوّن من Core وPlugins وThemes. أكتر مكان فيه ثغرات هو الـ Plugins القديمة أو المش متحدّثة.

**WPScan** 
أداة متخصصة في Kali بتكتشف الـ Plugins وإصداراتها واليوزرنيمز. مع الـ API Token بتقدر تشوف الثغرات المعروفة مباشرة.

**الخطوات بالترتيب:**
١ — سكان عام لمعرفة إصدار WordPress 
٢ — سكان للـ Plugins بالـ Aggressive Mode 
٣ — سكان للـ Usernames 
٤ — إضافة الـ API Token لمعرفة الثغرات 
٥ — استخدام searchsploit أو Metasploit للاستغلال

بكره لما الدكتور يكمل، هتكون عندك القاعدة دي وهيبني عليها. 💪