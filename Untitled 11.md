### أولاً: البرومبت (Prompt) الموجه لـ Claude

**العنوان: تحسين تجربة المستخدم (UI/UX) لمشروع Antonios عبر الـ Animations**

**سياق المشروع:** نظام إدارة كافيه وبلايستيشن مبني بـ PHP, MySQL, jQuery, و Bootstrap 4 (RTL). الملف الرئيسي للستايل هو `css/main_style.css`.

**المطلوب تنفيذ الخطوات التالية بدقة:**

**الخطوة 1: تحديث ملف `css/main_style.css`**

أضف التحريكات التالية مع مراعاة لغة RTL:

1. **تحريك الظهور (Fade-in):** كلاس `.page-fade` لإظهار محتوى الصفحات تدريجياً عند التحميل.
    
2. **تفاعلات البطاقات (Cards):** إضافة `transition` لبطاقات الأجهزة والطاولات لتعطي تأثير Zoom-in بسيط وظل مرتفع عند الـ `hover`.
    
3. **نبض الحالة النشطة:** كلاس `.status-pulse` لنقطة خضراء تنبض بجانب الأجهزة التي حالتها `active=1`.
    
4. **اهتزاز التنبيهات (Wiggle):** تحريك خاص للأيقونات عند وصول المخزون لـ 5 أو 2 (نظام checkAlerts).
    
5. **تطوير الـ Modals:** تعديل أنيميشن Bootstrap الافتراضي ليكون Slide-down مع Scale خفيف.
    

**الخطوة 2: تعديل ملف `index.php` و `cafe.php` (Javascript/jQuery)**

1. **عداد الأرقام (Counter):** إضافة كود jQuery لتحريك الأرقام في بطاقات "إجمالي المبيعات، الأرباح، والدرج المتوقع" لتبدأ من 0 وتتصاعد عند فتح لوحة الكافيه.
    
2. **ظهور الجداول (Staggered):** عند جلب بيانات `get_dashboard` عبر AJAX، اجعل صفوف الجدول تظهر بتتابع زمني سريع (Delay).
    
3. **تفاعل التشغيل:** عند الضغط على `makeonline.php` وظهور تحريك المربعات الخاص بي، أضف كوداً يمنع أي تحريك آخر (Hover) على الكرت حتى تنتهي العملية.
    

**الخطوة 3: القوائم (Sidebar/Navbar)**

1. أضف تأثير خط سفلي متحرك (Animated Underline) أو تغيير خلفية سلس عند الوقوف على روابط التنقيب في `index.php`.
   
   
   
   
   
   
### **Prompt لـ Claude: نظام "Antonios Pixel-Flow" المتكامل (تطوير شامل للملفات)**

**المطلوب:** تطوير نظام تشغيل الأجهزة في مشروع "Antonios" تقنياً وبصرياً. سأقوم بتزويدك بملفات المشروع كاملة، والمطلوب منك تعديل الأكواد داخلها وإعادة إرسال الملفات كاملة بعد دمج الميزات التالية:

**1. هندسة الواجهة (Spacing & 8-bit Grid):**

- قم بزيادة المسافات بين كروت الأجهزة (Margins) بمقدار **8-bit spacing** (حوالي 8px إلى 12px) لضمان وجود مساحة كافية لظهور "الوميض الخارجي" دون تداخل الكروت.
    

**2. مرحلة "تعبئة البكسل" (Pixel Loading Phase):**

- **المنطق:** بدلاً من تغيير لون الكرت فجأة عند التشغيل، طبق فكرة "التعبئة" (Filling).
    
- **عند البداية:** بمجرد نجاح `makeonline.php` عبر AJAX، يتحول الكرت للون **أبيض مضيء (Glowing White)**.
    
- **الأنيميشن:** تظهر **مكعبات برتقالية صغيرة (8-bit Style)** تتحرك بسرعة من اليسار إلى اليمين لتملأ الكرت بالكامل. هذه الحركة توحي بأن الجهاز "بدأ يحمل" أو "يتم تفعيله حالياً".
    
- **النهاية:** عند اكتمال التعبئة، يستقر الكرت على **اللون البرتقالي المتدرج (Radial Gradient Orange)**.
    

**3. نظام "الغبار التفاعلي" (Interactive Particles):**

- بعد اكتمال التعبئة، تظهر جزيئات "غبار" بيضاء دقيقة في الخلفية.
    
- **تفاعل الماوس:** الجزيئات يجب أن تتفاعل مع حركة الماوس داخل حدود الكرت (تنجذب أو تتحرك مع الماوس) لإعطاء لمسة حيوية.
    

**4. الوميض البرتقالي (Active Border Glow):**

- أي جهاز يحمل حالة `active=1` يجب أن يحيط بإطاره الخارجي **وميض نبضي (Pulse Glow) برتقالي** مستمر، يظهر بوضوح بفضل المسافات الـ 8-bit التي أضفناها.
    

**5. تعليمات التنفيذ التقنية (CRITICAL):**

- **التعامل مع الملفات:** قم بتعديل `index.php` و `css/main_style.css` وأي ملفات مرتبطة (مثل `devices.php`) لضمان عمل النظام ديناميكياً مع أي عدد من الأجهزة.
    
- **الطبقات (Z-Index):** يجب أن تظل بيانات الوقت، الحساب، وأزرار الإضافات في المقدمة (`z-index` مرتفع) والأنيميشن في الخلفية.
    
- **التفاعل:** استخدم `pointer-events: none;` لحاوية الأنيميشن لضمان عدم إعاقة الضغط على الأزرار.
    
- **المخرجات:** أريد منك تعديل الملفات التي سأرسلها لك وإعادة إرسال الكود الكامل لكل ملف بعد التعديل.




### **Prompt لـ Claude: إعادة تصميم شاملة لصفحة Setup مشروع Antonios (النسخة التفاعلية R2)**

**المطلوب:** تطوير صفحة الإعداد (`setup.php`) برمجياً وبصرياً لتتحول إلى واجهة "Terminal Control Center" احترافية. سأقوم بتزويدك بملف `setup.php` الحالي وكامل ملفات المشروع، والمطلوب منك إعادة صياغة ملف السيت أب ودمجه، ثم إرسال كافة ملفات المشروع معدلة وجاهزة للعمل.

**التعديلات المطلوبة في صفحة `setup.php`:**

**1. التصميم البصري (Cyberpunk / Matrix UI):**

- **الخلفية:** اجعل الخلفية سوداء بالكامل مع تأثير "Matrix Code" أو "Grid" خفيف جداً يظهر في الخلفية.
    
- **اللوجو التفاعلي (AA R2):** ضع لوجو النظام الجديد **(AA R2)** في منتصف الصفحة بشكل بارز.
    
- **أنيميشن اللوجو:** اجعل اللوجو يتمتع بتأثير "تنفس ضوئي" (Breathing Glow) متداخل بالألوان البرتقالية واللبنية (Cyan) ليعطي إيحاءً بأن النظام قيد التشغيل.
    
- **الألوان:** اعتمد الأخضر الفسفوري للنجاح، والبرتقالي المتوهج للتحذيرات، والأبيض المضيء للنصوص.
    

**2. شريط التحميل (8-bit Progress Bar):**

- استبدل شريط التحميل التقليدي بشريط **8-bit Style Loading Bar** كبير.
    
- **الأنيميشن:** اجعل الشريط يمتلئ بمكعبات برتقالية متوهجة تتحرك بتتابع سريع ليعكس هوية مشروع Antonios الجديدة.
    

**3. نظام سجل العمليات (Live Terminal Logs):**

- اجعل العمليات (مثل فحص الداتا بيز `antonios` ، التأكد من Python، وفحص الطابعة عبر QZ Tray) تظهر كأنها أوامر تُكتب في "Terminal" حقيقي بتأثير الكتابة السريعة.
    
- عند نجاح كل خطوة، تظهر بجانبها كلمة `[ SUCCESS ]` أو `[ INITIALIZED ]` بلون أخضر وامض.
    

**4. الذكاء البرمجي والتحقق:**

- حافظ على كافة الوظائف البرمجية الحالية، بما في ذلك التأكد من الـ 8 جداول الأساسية ، وفحص اتصال `dmgsfcon/dmgconsf.php` باستخدام المتغير `$dmgsf_connect`.
    
- أضف رسائل تنبيهية واضحة في حال وجود نقص في المتطلبات البرمجية.
    

**5. زر الانتقال (Launch System):**

- لا يظهر الزر إلا بعد وصول شريط التحميل لـ 100% واكتمال كافة الفحوصات.
    
- اجعل الزر يتمتع بـ "وميض نبضي" (Pulse Glow) برتقالي قوي، وعند الضغط عليه يتم الانتقال فوراً لصفحة `index.php`.
    

**التعليمات التقنية للمخرج النهائي:**

- **التكامل:** تأكد أن التعديل في `setup.php` لا يؤثر على مسار ملفات المشروع الأخرى.
    
- **الأداء:** استخدم CSS Animations و JavaScript خفيف لضمان سلاسة التحميل دون استهلاك موارد الجهاز.
    
- **التسليم:** أريد منك بعد الانتهاء إرسال كود ملف `setup.php` المطور، مع التأكيد على إرسال كافة ملفات المشروع معدلة لتعمل بتناغم مع النسخة الجديدة.





### **البرومبت النهائي والمطور (The Master Setup Prompt)**

**الموضوع:** تحويل ملف `setup.php` إلى نظام تثبيت ذاتي الأتمتة (Fully Automated Installer).

**المطلوب:** تعديل ملف `setup.php` وكامل ملفات المشروع لتنفيذ المهام التالية بدقة صفر خطأ:

**1. الأتمتة الكاملة لقاعدة البيانات (Auto-Import SQL):**

- بمجرد إنشاء قاعدة البيانات `antonios` (أو التأكد من وجودها)، قم برمجياً بفتح ملف الـ SQL الخاص بالمشروع (مثلاً `database.sql` أو المسار الذي سأحدده لك) وتنفيذ جميع الاستعلامات (Queries) بداخله تلقائياً باستخدام `multi_query` في PHP.
    
- **الهدف:** لا أريد الدخول إلى phpMyAdmin لاستيراد الجداول يدوياً؛ النظام يجب أن يجهز الداتا كاملة بمجرد انتهاء السيت أب.
    

**2. إدارة البرامج الخارجية والطباعة (Silent Dependencies Install):**

- **الفحص الذكي:** تأكد من وجود البرامج اللازمة للطباعة (مثل QZ Tray أو أي Driver محدد).
    
- **التحميل التلقائي:** في حال عدم وجودها، استخدم أوامر `exec()` أو `shell_exec()` في PHP لتحميل ملفات التثبيت (Installer) من روابط مباشرة أو من مجلد داخل المشروع، وبدء عملية التثبيت صامتاً (Silent Install) إن أمكن، أو توجيه المستخدم برابط تحميل مباشر مع فتح الملف تلقائياً بعد التحميل.
    
- تأكد من ضبط إعدادات جدار الحماية (Firewall) أو الصلاحيات اللازمة لعمل الطابعة برمجياً.
    

**3. نظام خيارات الطباعة (Printer Wizard):**

- بعد انتهاء المهام الأساسية، يظهر خيار تفاعلي للمستخدم: **"هل تريد إضافة طابعة جديدة الآن؟"**
    
- **في حال اختيار "لا":** يظهر فوراً زر **"Launch System"** الذي ينقل المستخدم للمشروع مع تفعيل أنيميشن "الانفجار البكسلي" (Pixel Explosion).
    
- **في حال اختيار "نعم":** يفتح النظام واجهة لإضافة الطابعة وتنسيق إعداداتها. وبعد الحفظ، يظهر زر الانتقال للمشروع.
    

**4. اختبار الطباعة الترحيبي (The Welcome Print):**

- بمجرد الضغط على زر "Launch System" والانتقال للمشروع، يجب أن ترسل الطابعة أمراً فورياً (First Print Job) يطبع الآتي: `Welcome to R2 Playstation` `Antonios ❤️` (بالإنجليزي بجانبها قلب).
    

**5. المخرجات التقنية:**

- أريد تعديل ملف `setup.php` بحيث يحتوي على هذه الحلقات البرمجية.
    
- تأكد من أن اللوجو (AA R2) يظل في وضع "التنفس الضوئي" أثناء هذه العمليات الخلفية.
    
- أرسل لي الملفات كاملة (Setup، ملفات الربط، وملفات الطباعة) جاهزة تماماً للعمل على جهاز جديد "خام".