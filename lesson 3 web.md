
---

**الملف من 31 لـ 55 بيتكلم عن 3 موضوعات كبار:**

أولاً — **Web Application Architecture** (معمارية تطبيق الويب): إزاي التطبيق متبني من جوا، مين بيكلم مين، وإيه دور كل جزء.

تانياً — **Web Application Technologies** (تقنيات تطبيق الويب): الأدوات والتقنيات اللي بتبني بيها الموقع من جهة الـ Client ومن جهة الـ Server، وإزاي البيانات بتتبادل بينهم.

تالثاً — **How Web Pages Are Rendered** (إزاي الصفحة بتتعرض): رحلة الطلب من المتصفح لحد ما الصفحة بتظهر قدامك.

ده مش كلام نظري بس — ده الأساس اللي هتبني عليه تفكيرك كـ Pentester، لأن لما تعرف كل جزء في التطبيق بيشتغل إزاي، هتعرف تدور على الثغرات في الأماكن الصح.

يلا نبدأ 👇

---

## 🔵 السلايدات 31-35 — Web Application Architecture

### معمارية تطبيق الويب

**الفكرة الأساسية:** قبل ما تختبر أي تطبيق، لازم تعرف هو متبني إزاي. لأنك لو مش عارف الأجزاء — مش هتعرف تدور على الثغرات في الأماكن الصح.

التطبيق مش كتلة واحدة — هو مجموعة أجزاء بتكلم بعض.
![[Pasted image 20260331183415.png]]
**مكونات التطبيق بالتفصيل:**

**User Interface — UI**
واجهة المستخدم الجزء اللي الناس بيشوفوه — الصفحات، الأزرار، النماذج. بيتبنى بـ HTML وCSS وJavaScript.

**Client-Side Technologies** 
تقنيات جانب العميل كل اللي بيتنفذ في متصفح المستخدم مباشرة بدون ما يروح للسيرفر.

**Server-Side Technologies**
تقنيات جانب السيرفر كل اللي بيتنفذ على السيرفر — PHP وPython وJava وRuby. هنا بيحصل المنطق الحقيقي للتطبيق.

**Web Server**
بيستقبل الطلبات من المتصفح ويرد عليها. أمثلة: Apache وNginx وIIS.

**Application Server**
 بينفذ كود الـ Server-Side ويتعامل مع قاعدة البيانات ويولّد المحتوى الديناميكي.

**Database**
قاعدة البيانات بتخزن كل بيانات التطبيق — بيانات المستخدمين، المحتوى، الإعدادات. أمثلة: MySQL وMSSQL وOracle.

**Application Logic**
 القواعد اللي بتحكم شغل التطبيق — التحقق من الهوية، التحقق من البيانات، قواعد الأمان.

---

## 🔵 السلايدات 36-39 — Client-Side vs Server-Side Processing

**الفكرة المهمة جداً للـ Pentester:** في حاجات بتتنفذ عند المستخدم (Client-Side) وحاجات بتتنفذ على السيرفر (Server-Side). الفرق ده مهم جداً لأنه بيحدد **إيه اللي تقدر تتلاعب فيه كمهاجم.
![[Pasted image 20260331183456.png]]
****النقطة الأهم للـ Pentester:** أي تحقق (Validation) موجود على الـ Client-Side بس — ده مش حماية حقيقية. المهاجم يقدر يفتح الـ Browser Developer Tools أو يستخدم Burp Suite ويتجاوزه في ثوانٍ. الحماية الحقيقية لازم تكون على الـ Server-Side.

---

## 🔵 السلايدات 40-50 — Web Application Technologies & Data Interchange

**الفكرة العامة:** دلوقتي بنعرف إيه التقنيات اللي بتبني بيها التطبيق، وإزاي الأجزاء دي بتتكلم مع بعض.

---

### تقنيات الـ Client-Side

**HTML** HyperText Markup Language 
لغة الترميز التشعبي — بتحدد هيكل الصفحة ومحتواها. الأساس الليكل حاجة بتتبني عليه.

**CSS** Cascading Style Sheets 
أوراق الأنماط المتتالية — بتتحكم في شكل وتصميم الصفحة، الألوان، الخطوط، التخطيط.

**JavaScript** 
جافاسكريبت — بتضيف التفاعل للصفحة. بتشتغل في المتصفح وبتعمل تحقق من البيانات من غير ما تروح للسيرفر.

**Cookies & Local Storage** 
الكوكيز والتخزين المحلي — بتخزن بيانات صغيرة في المتصفح. الـ Cookies بيستخدمهم كتير لـ Session Management.

---

### تقنيات الـ Server-Side

**Web Server** 
خادم الويب — بيستقبل طلبات HTTP ويرد عليها. أمثلة: Apache2 وNginx وMicrosoft IIS.

**Application Server**
خادم التطبيق — بيشغّل منطق التطبيق ويتكلم مع قاعدة البيانات.

**Database Server**
خادم قاعدة البيانات — بيخزن كل بيانات التطبيق. أمثلة: MySQL وPostgreSQL وMSSQL وOracle.

**Server-Side Scripting Languages**
لغات البرمجة النصية لجانب السيرفر — PHP وPython وJava وRuby.

---

### Data Interchange — تبادل البيانات

ده موضوع مهم جداً لأنك كـ Pentester هتتعامل معاه كتير.

**API** Application Programming Interface 
واجهة برمجة التطبيقات — طريقة التطبيق يتكلم بيها مع تطبيقات أو خدمات تانية. مثال: تطبيق موبايل بيكلم السيرفر عن طريق API.

**JSON** JavaScript Object Notation
صيغة تبادل البيانات الأخف والأسهل — بتشوفها كتير في ردود الـ APIs.

**XML** eXtensible Markup Language
لغة الترميز الموسّعة — صيغة تانية لتبادل البيانات. أقدم من JSON وبتستخدمها بعض الأنظمة القديمة (Legacy Systems).

**REST** Representational State Transfer 
نمط معماري لبناء الـ APIs — بيستخدم HTTP methods زي GET وPOST وPUT وDELETE. ده الأكثر استخداماً في الوقت الحالي.

**SOAP** Simple Object Access Protocol 
بروتوكول قديم لتبادل البيانات — بيستخدم XML وبتلاقيه في الأنظمة القديمة.

---

### Security Technologies — تقنيات الأمان

**SSL / Secure Sockets Layer - TLS**  / Transport Layer Security
بروتوكولات تشفير الاتصال — بيتأكدوا إن البيانات اللي بتتنقل بين المتصفح والسيرفر مشفرة. ده اللي بيخلي الـ URL يبدأ بـ HTTPS بدل HTTP.

**CDN** Content Delivery Network
شبكة توصيل المحتوى — بتوزع المحتوى الثابت (صور، CSS، JavaScript) على سيرفرات في أماكن مختلفة حول العالم عشان الموقع يبقى أسرع.

---

## 🔵 السلايدات 53-55 — إزاي الصفحة بتتعرض؟

**الفكرة:** لما بتكتب عنوان موقع في المتصفح — إيه اللي بيحصل بالظبط لحد ما الصفحة تظهر قدامك؟
![[Pasted image 20260331183638.png]]
**DOM** Document Object Model
نموذج كائن المستند — لما المتصفح بيقرأ الـ HTML بيحوّله لشجرة من العناصر في الذاكرة. الـ JavaScript بيتعامل مع الـ DOM ده عشان يعدّل الصفحة من غير ما يرجع للسيرفر.

**أهمية ده للـ Pentester:** لما المتصفح بيبني الـ DOM من الـ HTML — ده بالظبط هو المكان اللي هجمات الـ XSS بتتنفذ فيه. المهاجم بيحقن كود JavaScript بيتنفذ في الـ DOM بتاع الضحية.

---

## 📌 ملخص سريع — السلايدات 31-55

**معمارية التطبيق = 3 طبقات:** Client-Side ← السيرفر ← قاعدة البيانات

**القاعدة الذهبية:** أي حماية موجودة على الـ Client-Side بس — مش أمان حقيقي، المهاجم يتجاوزها بسهولة.

**التقنيات اللي لازم تعرفها:** HTML وCSS وJavaScript على الـ Client، وPHP وPython وJava على الـ Server، وMySQL وMSSQL لقواعد البيانات.

**تبادل البيانات:** JSON وXML للبيانات، وREST وSOAP للـ APIs، وHTTPS للتشفير.

**رحلة الصفحة:** طلب ← HTML ← JS ← DOM ← عرض

---

ده كان آخر محتوى في الملف الأول كامل. عايز نبدأ ملف الجلسة التانية؟ 🚀