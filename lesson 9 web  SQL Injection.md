الآن لدي كل المحتوى. سأكتب الشرح الشامل.

---

# 🛡️ SQL Injection — الشرح الكامل

---

## 📌 أولاً: الفكرة العامة للملفين

الملف الأول يشرح **المفاهيم الأساسية** لثغرة SQL Injection — ما هي، تاريخها، أنواعها، وكيف تعمل قواعد البيانات.
الملف الثاني يشرح **الجانب العملي** — كيف تبحث عن الثغرة، كيف تستغلها بكل نوع من أنواعها، وكيف تنتقل لـ NoSQL Injection.

بعد قراءة هذا الشرح ستكون قادراً على:
- تفهم لماذا توجد الثغرة أصلاً
- تعرف كل نوع وكيف يختلف عن التاني
- تعرف تبدأ الاستغلال العملي بيدك
- تفهم قواعد البيانات بالقدر الكافي لتكمل

---

## 📌 ثانياً: ما الذي يجب أن تأخذه من هذا الموضوع؟

- SQL Injection من أخطر الثغرات وأقدمها — موجودة منذ أوائل الـ 2000
- سببها الأساسي هو **عدم التحقق من مدخلات المستخدم** قبل إرسالها للقاعدة
- لها ثلاثة أنواع رئيسية: In-Band, Blind, Out-of-Band
- كل نوع له أسلوب استغلال مختلف
- موجودة في أي حقل إدخال يتعامل مع قاعدة البيانات

---

## 📌 ثالثاً: الشرح الكامل باللغة العربية

---

### 🔵 الجزء الأول: ما هي ثغرة SQL Injection؟

تخيّل إن عندك موقع بيه نموذج لتسجيل الدخول (login form). المستخدم بيكتب اسم المستخدم وكلمة السر، والموقع بيبعت هذه المعلومات لقاعدة البيانات (database — قاعدة البيانات) عشان يتحقق منها.

الكود بتاع الموقع بيكون شكله كده في الخلفية (server-side code — الكود في الخادم):

```sql
SELECT * FROM users WHERE username = 'ahmed' AND password = '1234';
```

الجملة دي معناها: "اجلب البيانات من جدول المستخدمين حيث الاسم = ahmed وكلمة السر = 1234".

**المشكلة:** لو الموقع بياخد اللي المستخدم كتبه ويحطه مباشرة في الجملة دي بدون ما يتحقق منه، ممكن المهاجم يكتب بدل اسمه:

```
' OR '1'='1
```

فتبقى الجملة كده:

```sql
SELECT * FROM users WHERE username = '' OR '1'='1' AND password = '...';
```

الشرط `'1'='1'` دايماً صح، فالقاعدة بترجع كل المستخدمين، والمهاجم بيدخل من غير ما يعرف أي كلمة سر.

**ده بالضبط هو SQL Injection (إس-كيو-إل إنجكشن)** — حقن (injection — حقن) جمل SQL خبيثة جوه مدخلات الموقع.

---

### 🔵 الجزء الثاني: ليه الثغرة بتتكون؟

الثغرة بتتكون لسبب واحد بسيط: **الـ Dynamic Queries (الاستعلامات الديناميكية)**.

بدل ما تبقى الجملة ثابتة (static)، الموقع بيبني الجملة باستخدام مدخلات المستخدم مباشرة:

```php
$id = $_GET['id'];  // المستخدم بيبعت قيمة id في الـ URL
$query = "SELECT Name, Description FROM Products WHERE ID='$id';";
```

لو المستخدم بعت `id=1` — تمام.
لو المستخدم بعت `id=' OR '1'='1` — **كارثة**.

الحل الصحيح هو استخدام Prepared Statements (الاستعلامات المُجهّزة مسبقاً) أو تنظيف المدخلات — لكن ده دور المطوّر مش المهاجم 😄

---

### 🔵 الجزء الثالث: قواعد البيانات — فهم ضروري

قبل ما تعرف تهاجم، لازم تعرف تهاجم إيه.

**Database (قاعدة البيانات):** مكان منظم لتخزين البيانات.

**DBMS (دي-بي-إم-إس) 
— نظام إدارة قواعد البيانات:** البرنامج اللي بيتحكم في القاعدة. أمثلة:
- **MySQL (ماي-إس-كيو-إل):** الأكثر شيوعاً في المواقع — مجاني ومفتوح المصدر
- **MSSQL / SQL Server:** من Microsoft — شائع في الشركات
- **PostgreSQL (بوست-جريس-إس-كيو-إل):** متقدم ومفتوح المصدر
- **Oracle (أوراكل):** تجاري — للشركات الكبيرة

**Relational Database (قاعدة البيانات العلائقية):** 
بتخزن البيانات في جداول (tables) فيها صفوف (rows) وأعمدة (columns)، والجداول مربوطة ببعض عن طريق مفاتيح (keys — مفاتيح).

مثال: جدول `Students`، جدول `Courses`، وجدول `Student_Courses` بيربطهم ببعض.

**NoSQL (نو-إس-كيو-إل):** 
نوع تاني من القواعد مش بيستخدم الجداول. بدل كده بيستخدم مستندات (documents) بصيغة JSON. أشهر مثال هو **MongoDB (مونجو-دي-بي)**.

---

### 🔵 الجزء الرابع: أساسيات SQL اللي محتاجها للـ Injection

#### أوامر SQL الأساسية:

| الأمر | الوظيفة |
|-------|---------|
| `SELECT` | جلب بيانات من القاعدة |
| `FROM` | تحديد الجدول |
| `WHERE` | شرط الاختيار |
| `UNION` | دمج نتائج استعلامين |
| `INSERT` | إضافة صف جديد |
| `UPDATE` | تعديل بيانات موجودة |
| `DELETE` | حذف بيانات |

#### أمثلة عملية:

```sql
-- جلب اسم وسعر منتج رقم 9
SELECT name, description FROM products WHERE id=9;

-- دمج نتيجتين باستخدام UNION
SELECT name FROM products WHERE id=9 UNION SELECT username FROM users;

-- التعليقات في SQL (مهمة جداً في الـ Injection!)
SELECT field FROM table; -- هذا تعليق يتجاهل ما بعده
SELECT field FROM table; # هذا أيضاً تعليق
```

> ⚠️ **ملاحظة مهمة:** التعليقات (`--` و `#`) في SQL بتُلغي كل اللي بعدها في نفس السطر. المهاجم بيستخدمها عشان يُلغي باقي الاستعلام الأصلي بعد ما يحقن الجزء بتاعه.

---

### 🔵 الجزء الخامس: أنواع SQL Injection

الثغرة بتنقسم لثلاثة أنواع رئيسية:

```
SQL Injection
├── In-Band SQLi (إن-باند إس-كيو-إل-آي)
│   ├── Error-Based SQLi (إيرور-بيزد)
│   └── Union-Based SQLi (يونيون-بيزد)
├── Blind SQLi (بلايند إس-كيو-إل-آي)
│   ├── Boolean-Based SQLi (بوليان-بيزد)
│   └── Time-Based SQLi (تايم-بيزد)
└── Out-of-Band SQLi (أوت-أوف-باند)
```

---

#### 🟢 النوع الأول: In-Band SQL Injection

**الفكرة:** المهاجم بيبعت الهجوم ويستقبل النتيجة عن طريق **نفس القناة** — يعني نفس الاتصال HTTP.

**تشبيه:** تبعت سؤال في رسالة واحدة واتبعتلك الإجابة في نفس الرسالة.

##### أ) Error-Based SQL Injection (إيرور-بيزد):

**الفكرة:** المهاجم بيحقن جملة SQL خاطئة عن قصد عشان يخلي قاعدة البيانات ترجع **رسالة خطأ** (error message). الرسالة دي ممكن تكشف معلومات عن بنية القاعدة.

مثال: لو حقنت `'` في حقل إدخال فظهرت رسالة زي:

```
You have an error in your SQL syntax near '1'' at line 1
```

ده معناه إن الموقع مش بيتحقق من المدخلات وقاعدته MySQL — **معلومة ذهبية** للمهاجم.

**خطوات Methodology (منهجية):**
1. ابحث عن parameter قابل للحقن (URL, form, cookie)
2. احقن payload مكسور عمداً (زي `'`)
3. لاحظ رسالة الخطأ — بتكشف نوع القاعدة ومعلوماتها
4. استخدم الـ errors عشان تجمع معلومات عن الجداول والأعمدة
5. استخرج البيانات المطلوبة

---

##### ب) Union-Based SQL Injection (يونيون-بيزد):

**الفكرة:** المهاجم بيستخدم أمر `UNION` عشان **يدمج** نتائج استعلام تاني مع الاستعلام الأصلي، وبالتالي يقرأ بيانات من جداول مش المفروض يوصلها.

**مثال:** الاستعلام الأصلي:

```sql
SELECT id, name FROM users WHERE id = '1'
```

المهاجم بيحقن:

```sql
' UNION SELECT credit_card_number, 'hacked' FROM credit_cards --
```

فيبقى الاستعلام كامل:

```sql
SELECT id, name FROM users WHERE id = '' UNION SELECT credit_card_number, 'hacked' FROM credit_cards --'
```

والنتيجة: أرقام الكريدت كارد بتظهر في صفحة الموقع!

> ⚠️ **شرط مهم جداً:** الـ UNION بيشترط إن الاستعلامين يكون عندهم **نفس عدد الأعمدة** ونفس نوع البيانات. لازم تحدد عدد الأعمدة الأول.

**خطوات Methodology:**
1. ابحث عن نقطة الحقن
2. اكتشف عدد الأعمدة (بـ `ORDER BY 1`, `ORDER BY 2`... لحد ما يحصل خطأ)
3. اكتشف أي عمود بيظهر في الصفحة
4. استخدم `UNION SELECT` عشان تجيب بيانات من جداول تانية
5. تعرّف على بنية القاعدة (اسم القاعدة، الجداول، الأعمدة)
6. استخرج البيانات الحساسة

---

#### 🟡 النوع الثاني: Blind SQL Injection

**الفكرة:** الموقع **مش بيرجع** لا نتائج القاعدة ولا رسائل أخطاء في صفحة الاستجابة. المهاجم عمياً — مش بيشوف حاجة مباشرة.

**تشبيه:** بتطرح سؤال والشخص التاني بيجاوبك بـ "أيوه" أو "لأ" بس — محتاج تبني صورة كاملة من الأجوبة البسيطة دي.

##### أ) Boolean-Based SQL Injection (بوليان-بيزد):

**الفكرة:** المهاجم بيحقن شروط (conditions — شروط) بتكون إما True (صح) أو False (غلط)، وبيلاحظ إذا كانت الصفحة بترجع بـ"شكل مختلف" — مش لازم بيانات، مجرد فرق في السلوك.

مثال: المهاجم يحقن:

```sql
' OR '1'='1   --> الصفحة تظهر (TRUE)
' OR '1'='2   --> الصفحة تختفي أو تتغير (FALSE)
```

ومن الفرق ده بيعرف يسأل أسئلة متقدمة زي:

```sql
' OR LENGTH(database()) > 5 --   --> طول اسم القاعدة أكبر من 5؟
' OR SUBSTRING(database(),1,1) = 'a' --  --> أول حرف في اسم القاعدة هو 'a'؟
```

بيكرر العملية دي لحد ما يجمع كل الحروف — **بطيئة لكن فعّالة**.

**استخدام مشهور: Authentication Bypass (تخطي المصادقة):**

```sql
' OR '1'='1
```

في خانة اسم المستخدم يخلي الشرط دايماً True، فيدخل من غير كلمة سر.

---

##### ب) Time-Based SQL Injection (تايم-بيزد):

**الفكرة:** المهاجم بيحقن أمر `SLEEP()` في MySQL (أو `WAITFOR DELAY` في MSSQL) عشان يخلي القاعدة **تتأخر** في الرد. لو الرد اتأخر = الشرط True. لو الرد جاء بسرعة = الشرط False.

مثال:

```sql
' OR SLEEP(5) -- '
```

فيبقى الاستعلام:

```sql
SELECT * FROM users WHERE username = '' OR SLEEP(5) -- ' AND password = '...';
```

لو الموقع اتأخر 5 ثواني في الرد → الحقن نجح والنقطة دي vulnerable!

**الفرق بين Boolean وTime-Based:**

| | Boolean-Based | Time-Based |
|---|---|---|
| كيف نعرف إن الشرط True؟ | الصفحة بتتغير | الرد بيتأخر |
| يُستخدم لو | الصفحة بتتغير بصرياً | الصفحة ثابتة تماماً |

---

#### 🔴 النوع الثالث: Out-of-Band SQL Injection

**الفكرة:** المهاجم بيحقن عبر قناة وبيستقبل النتيجة عبر **قناة تانية** تماماً — مثلاً DNS request أو HTTP request لسيرفر خارجي بيتحكم فيه المهاجم.

**ده النوع الأنادر** — بيتطلب إن القاعدة تملك صلاحيات معينة تقدر تبعت requests للخارج.

---

### 🔵 الجزء السادس: كيف تبحث عن الثغرة؟ (الجانب العملي)

#### 1. الحقول المحتمل وجود الثغرة فيها:
- **Login forms (نماذج تسجيل الدخول):** username وpassword
- **Search boxes (صناديق البحث):** أي حقل بحث
- **URL parameters (باراميترات الـ URL):** زي `?id=1` أو `?category=shoes`
- **Form fields (حقول النماذج):** نماذج التسجيل، التواصل، التعليقات
- **Cookies (الكوكيز):** لو البيانات فيها بتتبعت للقاعدة
- **Hidden fields (الحقول المخفية):** حقول HTML مخفية في الـ source code

#### 2. أول خطوة: اختبار بالأحرف الخاصة

دايماً ابدأ بحقن أحرف خاصة وشوف ردة فعل الموقع:

```
'          --> إذا جاء error → الموقع vulnerable!
''         --> True condition
"          --> اختبار الـ double quote
--         --> تعليق SQL
#          --> تعليق MySQL
' OR '1'='1    --> Always True
' OR '1'='2    --> Always False
```

> ⚠️ **قاعدة ذهبية:** اختبر **injection واحد في كل مرة**، لأنك مش هتعرف أيهم نجح لو اختبرتهم مع بعض!

#### 3. Integer vs String Based Injection

**Integer-Based (باراميتر رقمي):** زي `?id=1`

```sql
AND 1   --> True
AND 0   --> False
AND 1=1 --> True
1*56    --> يرجع 56 لو vulnerable
```

**String-Based (باراميتر نصي):** زي `?name=ahmed`

```sql
'   --> يكسر الـ query
''  --> True
' OR '1'='1    --> دايماً True
```

#### 4. Database Fingerprinting (تحديد نوع القاعدة)

كل قاعدة بيانات بترجع error message مختلفة:

- **MySQL:** `You have an error in your SQL syntax...`
- **MSSQL:** `Incorrect syntax near...`
- **PostgreSQL:** `ERROR: unterminated quoted string...`

معرفة نوع القاعدة مهمة لأن الـ payloads بتختلف من قاعدة لأخرى!

---

### 🔵 الجزء السابع: NoSQL Injection

بما إن المواقع الحديثة بتستخدم MongoDB وأمثالها، في نوع تاني من الحقن اسمه **NoSQL Injection (نو-إس-كيو-إل إنجكشن)**.

MongoDB بتستخدم queries بصيغة JSON بدل SQL:

```javascript
// استعلام عادي
db.users.findOne({ username: "ahmed", password: "1234" })
```

المهاجم ممكن يحقن بدل القيمة العادية **MongoDB operators (أوبيريتورز)**:

```javascript
// حقن خبيث - المهاجم بيبعت:
username: { $ne: "" }   // $ne = not equal to
// معناه: جيبلي أي مستخدم اسمه مش ""  → أول مستخدم في القاعدة!
```

**أمثلة على payloads موجودة في الملف:**

| الـ Payload | الوظيفة |
|------------|---------|
| `username[$ne]=1&password[$ne]=1` | Authentication Bypass |
| `username[$regex]=^adm&password[$ne]=1` | جيب يوزر اسمه بيبدأ بـ "adm" |
| `username[$regex]=.{25}&pass[$ne]=1` | اكتشف طول قيمة معينة |
| `username[$eq]=admin&password[$ne]=1` | Equals to |

---

### 🔵 الجزء الثامن: الأدوات المستخدمة

**يدوياً:**
- **Burp Suite (بيرب سويت):** لالتقاط الـ HTTP requests وتعديلها
- **OWASP ZAP (أو-واسب زاب):** نفس وظيفة Burp لكن مجاني بالكامل

**أوتوماتيك:**
- **SQLMap (إس-كيو-إل-ماب):** أداة قوية جداً بتـ automate عملية اكتشاف واستغلال SQL Injection — الأستاذ هيشرحها لاحقاً

**مراجع مهمة:**
- [PortSwigger SQLi Cheat Sheet](https://portswigger.net/web-security/sql-injection/cheat-sheet)
- [PayloadBox SQL Injection List](https://github.com/payloadbox/sql-injection-payload-list)

---

## 📌 رابعاً: جدول المصطلحات الموحّد

| English Term | المعنى بالعربية | النطق |
|---|---|---|
| SQL Injection | حقن SQL | إس-كيو-إل إنجكشن |
| SQLi | اختصار SQL Injection | إس-كيو-إل-آي |
| Database | قاعدة البيانات | — |
| DBMS | نظام إدارة قواعد البيانات | دي-بي-إم-إس |
| RDBMS | نظام إدارة قواعد البيانات العلائقية | آر-دي-بي-إم-إس |
| Relational Database | قاعدة البيانات العلائقية | — |
| NoSQL | قواعد بيانات غير علائقية | نو-إس-كيو-إل |
| MySQL | قاعدة بيانات MySQL | ماي-إس-كيو-إل |
| PostgreSQL | قاعدة بيانات PostgreSQL | بوست-جريس-إس-كيو-إل |
| MongoDB | قاعدة بيانات MongoDB | مونجو-دي-بي |
| In-Band SQLi | حقن داخل نفس القناة | إن-باند |
| Out-of-Band SQLi | حقن بقناة مختلفة | أوت-أوف-باند |
| Blind SQLi | حقن أعمى | بلايند |
| Error-Based SQLi | حقن مبني على رسائل الخطأ | إيرور-بيزد |
| Union-Based SQLi | حقن مبني على UNION | يونيون-بيزد |
| Boolean-Based SQLi | حقن مبني على الشروط المنطقية | بوليان-بيزد |
| Time-Based SQLi | حقن مبني على التأخير الزمني | تايم-بيزد |
| Dynamic Query | استعلام ديناميكي | — |
| Payload | الحمولة / الكود الخبيث المحقون | — |
| Injection Point | نقطة الحقن | — |
| Authentication Bypass | تخطي المصادقة | أوثنتيكيشن باي-باس |
| Data Exfiltration | سرقة/استخراج البيانات | داتا إكسفيلتريشن |
| Database Fingerprinting | تحديد نوع القاعدة | فينجر-برينتينج |
| UNION Operator | أمر الدمج في SQL | — |
| SLEEP() | دالة التأخير في MySQL | — |
| Prepared Statements | استعلامات مُجهّزة (الحل الآمن) | بري-بيرد ستيتمنتس |
| NoSQL Injection | حقن في قواعد NoSQL | — |
| MongoDB Operators | أوامر MongoDB ($ne, $gt...) | — |
| OWASP ZAP | أداة اختبار الأمان | أو-واسب زاب |
| SQLMap | أداة أتمتة SQL Injection | إس-كيو-إل-ماب |
| Burp Suite | أداة اعتراض HTTP | بيرب سويت |

---

## 📌 خامساً: ملخص نقاط الأساسية والخلاصة العملية

**لماذا توجد الثغرة؟**
← الموقع بياخد مدخل المستخدم ويحطه مباشرة في جملة SQL بدون تنظيف.

**كيف تكتشفها؟**
← ابدأ بـ `'` في أي حقل — لو جاء error أو تغيير في السلوك = vulnerable.

**كيف تختار نوع الاستغلال؟**

| الموقف | النوع المناسب |
|--------|--------------|
| الموقع بيرجع بيانات مباشرة أو errors | Error-Based أو Union-Based |
| الموقع بيتغير بصرياً (بيظهر/بيختفي) | Boolean-Based |
| الصفحة ثابتة تماماً - لا فرق | Time-Based |
| القاعدة NoSQL (MongoDB) | NoSQL Injection |

**تسلسل التفكير العملي:**
1. شوف الحقول — أي حاجة بتتبعت للسيرفر
2. اختبر بـ `'` — شاهد ردة الفعل
3. حدد نوع الـ parameter (رقم أم نص)
4. حدد نوع القاعدة (MySQL, MSSQL...) من الـ error
5. اختار نوع الاستغلال المناسب
6. ابدأ بالـ payloads المناسبة

**القادم في المحاضرات:**
الأستاذ هيأخذك على الـ labs العملية لكل نوع على حدة، وبعدين SQLMap لأتمتة كل العملية.